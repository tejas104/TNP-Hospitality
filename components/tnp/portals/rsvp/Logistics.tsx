'use client';

import type { SectionProps } from './types';
import { StatePanel } from './ui';

export function TravelSection(_props: SectionProps) {
  return (
    <StatePanel title="Not built in this checkpoint">
      <p>This screen is delivered in the milestone’s second prompt. Nothing here is functional yet.</p>
    </StatePanel>
  );
}

export function MovementsSection(_props: SectionProps) {
  return (
    <StatePanel title="Not built in this checkpoint">
      <p>This screen is delivered in the milestone’s second prompt. Nothing here is functional yet.</p>
    </StatePanel>
  );
}
