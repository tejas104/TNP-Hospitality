# TNP-FOUND-01 — one bounded correction cycle

Correction packet issued 2026-09-17T01:15:41+05:30. Original TASK remains TNP-FOUND-01; this is not a new parallel writer. Responsible human Anjaneya; original B session01a0aa5f-c6ee-76e2-93db-6b50e124d174, DESKTOP-DL9FDM7, Codex gpt-5.6-sol/high. Exact reviewed BASE77780970a8b0fdb52e1de70d3b35469fa78cb114; reviewed HEAD 502422c038c9292f4946b65c00ea0b2618515a9f; S0 checkpoint d5be3bbee463538c17365708ca1415ffe5e4323d.
Worktree D:\TNP-worktrees\TNP-FOUND-01, branch codex/tnp-foundation. P verified clean HEAD and remote match before reopening. Continue from reviewed HEAD; do not reset to metadata main, merge main into the feature branch merely for these instructions, or rewrite reviewed commits. Read this packet through git show origin/main:docs/reviews/TNP-FOUND-01-FIX-01.md after fetching origin. Current metadata revision is supplied in P's handoff after commit, not self-embedded.
Writer lease REOPENED only for original B and these accepted findings; no other app writer. Shared ownership stays exclusive. Consumer tasks remain unleased until reviewed foundation integration.
Mode synthetic local preview; risk shared typed interface and attendance/allocation/financial simulation. No production provider/authorization implementation or client-policy changes authorized. Existing package/framework restrictions remain.
Reviewers at fixed correction HEAD: Sonnet on Kartik Laptop2 focuses mobile controls/copy and regression; independent Sol on Kartik Laptop2 focuses types/state/finance/scoping. These independent reviewers may continue their own review contexts; they are not builders. Human Kartik plus both domain owners before integration. Original Astra requirement is satisfied by the explicitly approved independent Sol substitution, not by falsely labelling this an Astra review.

## Received evidence
Exact supplied Sol and Sonnet report bytes are archived under received/foundation-01-*.txt and hashed in received/manifest.json. Sol architecture gate FAIL with four P1 defects; Sonnet CHANGES REQUESTED with reproduced mobile blocking overlay. Passing9tests/typecheck/build at the old SHA did not cover these regressions. P source inspection confirms untyped mutate, unguarded respond and full-rate exception earning creation; other reproductions are independently supplied reviewer evidence until builder reruns them.

## Accepted corrections and required regression evidence
1. SOL1 typed mutation boundary: define a closed operation-to-payload/result map or equivalent discriminated typed methods. Unknown operations and missing/wrong fields must fail TypeScript compilation; success values must have usable inferred types without consumer casts. Keep runtime validation for malformed inputs. Add compile-time negative and positive assertions; tests/preview-contract.types.ts is explicitly added to this correction's owned paths and checked by existing tsc --noEmit. Do not solve with any or unknown result casts at the public boundary.
2. SOL2 response reactivation: every transition that reacquires/resumes a reservation must reuse allocation-state, current capacity, worker eligibility and overlap checks. Replaced/inactive assignments must not reactivate through respond. Reproduce quantity2 -> decline -> another worker fills -> original coming; reject without changing records. Add overlap and ineligible reactivation cases, repeated response behavior and unchanged valid response paths. This is in-process preview behavior, not production transaction certification.
3. SOL3 attendance/earnings: require an active eligible confirmed assignment at attendance recording; invalidate passes for declined/nonresponse/replaced assignments. Preserve the client-required missing-GPS path for otherwise eligible workers: record its exception evidence, do NOT silently treat it as verified attendance or immediately approvable full pay. Distinguish pending verification/estimated amounts from earned/payable amounts. Use immutable assignment rate snapshots (including seeded assignments); changing a position rate later must not change booked pay. Correction to absent/exception must invalidate dependent unapproved earnings or require explicit review, retain original evidence/audit and prevent downstream approval/payout of invalid earnings. Do not silently rewrite already-approved/processing/paid ledgers; reject and surface a pending adjustment/review case under unresolved production DEC-16. Test declined/replaced passes, missing/denied/outside evidence, absence corrections and rate changes. Replace the existing test that certifies unconditional full pay for gps-missing.
4. SOL4 / SONNET2 payout scope: listPayouts(workerId) must derive or explicitly carry correct worker ownership; unknown/no-earning worker returns empty. Shared/batched payout representations must not leak other workers' identifiers or amounts through the worker view. Preserve the intended unfiltered Operations query separately; do not remove the required worker query to avoid the finding. Test multiple workers and no-earning worker.
5. SOL5 distinct approvals: record and assert two distinct sample approvers with sequential evidence. Keep production separation-of-duties policy pending; label sample actor assumptions. Verify out-of-order/repeated approval behavior and the corrected invalid-attendance guard.
6. SOL6 truthful copy: replace Operations sidebar Live Workforce with explicit preview/sample wording; preserve layout and destination.
7. SONNET1 mobile obstruction: fix fixed PreviewControls/warning layout so bottom primary actions and status content remain visible and pointer/keyboard reachable at390x844. Scope to owned PreviewControls/AppShell; use original S0 globals ownership only if truly necessary and explain. Reproduce /client Submit Requirement and /freelancer Show Sample Scan Result after real max scroll. Verify center-point hit testing reaches the button or its child, actual click delivery (without claiming deferred business behavior implemented), and final planner timeline visibility. Check all five routes at390x844 and1440x900; warning/control overlap, Reset focus/reload and no overflow. Do not hide the required warning as a workaround.
8. SONNET observation attendance correction evidence: accept supported caller evidence states without silently coercing recorded/gps-denied into gps-missing, or reject unsupported input with a clear typed error. Preserve old evidence and reason. This is correction correctness, not an approved new business policy.

## Owned paths and boundaries
Use the original foundation-owned paths needed by these findings: lib/contracts/preview.ts; lib/demo/service.ts; lib/demo/scenario.ts; lib/demo/store.ts and lib/services/preview.ts only if typed integration requires; tests/preview-contract.test.mjs; NEW tests/preview-contract.types.ts; components/tnp/shared/PreviewControls.tsx; components/tnp/AppShell.tsx; Operations component copy only. Original ownership constraints apply to any supporting change; do not expand features, edit shared docs/registers, package/lockfiles, providers, arbitrary config or other consumer workflows. Existing ordinary/reset mechanics passed review; preserve their regression coverage and avoid unrelated redesign.
Port3102 and dedicated synthetic profile may be reclaimed after checking availability. Do not stop unknown processes. One heavy build per laptop. No actual server or review session is claimed running by this packet.

## Execution and checks
This is a correction cycle, not an initial launch: original reviewed HEAD is the start point; use current remote metadata to confirm reopened lease. If unrelated commits/dirty files appear, inspect and report; never reset them away. No fresh SOURCE_SHA/LAUNCH_SHA equality requirement replaces the existing reviewed application branch.
Run npm run lint; npx --no-install tsc --noEmit (includes compile-time contract assertions); node --experimental-strip-types --test tests/preview-contract.test.mjs; npm run build:vercel; python docs/checks/verify_bootstrap.py --provenance-only; git diff --check. Existing dependencies may be reused within this worktree; npm ci only if installation needs repair, never another worktree's node_modules. Run the explicit browser cases above with honest runtime evidence.
Commit meaningful fixes on codex/tnp-foundation and non-force push that branch only. No main merge/push/deployment, app writers or next task. Return per-finding disposition, targeted regression results, all required checks, actual model/session and final fixed SHA; pause lease again. One focused re-review of changed areas and retained critical regressions follows, not a repeat historical-doc audit. Consumer implementations stay blocked until that foundation gate passes.
