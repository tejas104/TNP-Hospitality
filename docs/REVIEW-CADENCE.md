# Milestone reviews and checkpoint sharing

Current 20-day release cadence: use consolidated fixed-SHA review windows on Days 5, 9 and 13, with focused correction/re-review inside the same window. Day 14 is code/schema/dependency/content freeze. This compression reduces repeated setup, not review depth for security, tenancy, concurrency, attendance, finance, provider or rollback risk. A blocking finding pauses dependent integration even if it threatens the Day-20 target.

User requested fewer reviews, faster implementation and branch sharing for architect inspection. This replaces per-slice formal reviews where an explicitly issued milestone contract groups the slices. It never makes a Draft task Ready.

Foundation milestone TNP-FOUND-01: B performs S0 then S1 sequentially in ONE isolated branch/worktree. S0's checked commit is an internal checkpoint, not a separately merged dependency. B may continue to S1 after S0 checks pass within the same milestone lease. No external consumers run until the entire foundation is reviewed, approved and integrated. S0/S1 task files remain specification/checklists; only FOUND-01 is dispatched.
Frontend milestones: after foundation, P groups A-01/A-02, B-01, C-01..03 and D-01..03 into explicit per-lane launch contracts. No old per-slice task launches automatically. Initially one builder per human; review each completed lane milestone, with early escalation for shared-contract or risk changes. I01 remains integrated browser acceptance before client readiness claims.
Builders still run exact applicable checks and commit meaningful checkpoints. A push is evidence sharing, not approval. Each Ready packet authorizes only that builder's named remote branch, without force-push, secrets, main merge or deployment. P fetches fixed SHAs on demand; no automatic remote monitoring is claimed. Developers fetch before launch and before an agreed integration; they do not blindly pull other lanes into active work or rebase reviewed SHAs.
Models directed by the user as of 2026-09-18: `gpt-6-astra` authors new homepage, Client, Freelancer, RSVP, public service-page and related UI/frontend work. Platform, security, data, Operations and finance use the model named by their Ready contract. Codex-authored work normally receives a fresh Claude fixed-SHA review; high-risk architecture/security/concurrency/payments/financial work also receives fresh independent Sol review. Model switching must be disclosed and actual runtime evidence recorded; a requested Astra assignment is not proof that Astra ran.
Review is after a major completed milestone, not after a number of chat prompts. Foundational architecture and production-sensitive identity, allocation/concurrency, attendance integrity and money flows keep independent review before dependent release/integration. Inherited lint is never labelled green. Effective 2026-09-18, Kartik is the sole human fixed-SHA reviewer for future milestones; Anjaneya remains the product/content/visual decision owner. Independent AI and specialist gates remain mandatory and authors cannot self-certify.

## Two-task batching clarification
User requested review after two task prompts per lane. After foundation integration, group up to two compatible, explicitly contracted frontend slices under one milestone lease, with a checked/pushed first checkpoint and one formal review after the second. Do not invent a second task merely to reach a prompt count. This does not waive a failed shared prerequisite or high-risk gate. Current foundation reviews failed on shared behavior, so the authorized next application work is original B correction and focused re-review. A/C/D wait for the corrected integrated interface.
