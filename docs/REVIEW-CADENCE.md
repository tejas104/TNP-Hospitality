# Milestone reviews and checkpoint sharing

User requested fewer reviews, faster implementation and branch sharing for architect inspection. This replaces per-slice formal reviews where an explicitly issued milestone contract groups the slices. It never makes a Draft task Ready.

Foundation milestone TNP-FOUND-01: B performs S0 then S1 sequentially in ONE isolated branch/worktree. S0's checked commit is an internal checkpoint, not a separately merged dependency. B may continue to S1 after S0 checks pass within the same milestone lease. No external consumers run until the entire foundation is reviewed, approved and integrated. S0/S1 task files remain specification/checklists; only FOUND-01 is dispatched.
Frontend milestones: after foundation, P groups A-01/A-02, B-01, C-01..03 and D-01..03 into explicit per-lane launch contracts. No old per-slice task launches automatically. Initially one builder per human; review each completed lane milestone, with early escalation for shared-contract or risk changes. I01 remains integrated browser acceptance before client readiness claims.
Builders still run exact applicable checks and commit meaningful checkpoints. A push is evidence sharing, not approval. Each Ready packet authorizes only that builder's named remote branch, without force-push, secrets, main merge or deployment. P fetches fixed SHAs on demand; no automatic remote monitoring is claimed. Developers fetch before launch and before an agreed integration; they do not blindly pull other lanes into active work or rebase reviewed SHAs.
Models preferred by user: Sol for A/B/D, Sonnet for C and routine Claude review, subject to actual session availability. Model switching during a review must be disclosed. User explicitly approved "Yes—use Sol + Sonnet" on 2026-09-16. Fresh independent Sol now replaces mandatory Astra review, including this bootstrap; Sonnet supplies the separate Claude review. No old interrupted review is retroactively passed. Record actual selected models; do not claim Sol is Astra.
Review is after a major completed milestone, not after a number of chat prompts. Foundational architecture and production-sensitive identity, allocation/concurrency, attendance integrity and money flows keep independent review before dependent release/integration. Inherited lint is never labelled green. Human reviewers retain approval and integration duties.
