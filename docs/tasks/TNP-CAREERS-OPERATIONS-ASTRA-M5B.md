# TNP-CAREERS-OPERATIONS-ASTRA-M5B - server-backed profile adoption

Status: DRAFT / DEPENDENCY AND REVIEW CAPACITY HOLD. No writer, branch, worktree, port, Ready SHA, reviewer appointment or push authority.

Human owner and fixed-SHA acceptance reviewer: Kartik. Required UI author: verified gpt-6-astra. External Claude review plus independent Sol privacy/API review are required.

## Prerequisites

The client-demo M5, shared themes, Careers M5a, worker access foundation and worker-profile API must each be reviewed, accepted and integrated at exact recorded SHAs. DEC-33 field matrix must be approved. Private upload and payout controls remain disabled until a later sensitive milestone.

## Result

Replace the synthetic selected-persona profile path with real session-backed own-profile reads and writes. Remove only known browser-local application-draft keys containing personal data during the real-mode transition; never clear unrelated browser storage. Show save, stale revision, retry, inactive membership, consent and empty states without presenting demo data as server truth.

Connect the M5 admin console's applications and "Freelancers & ratings" sections through a narrow worker projection adapter in components/tnp/portals/admin/AdminSections.tsx and adminData.ts, or their reviewed extracted successors. An ordinary co-admin sees only staffing fields. Verifier/Finance panels remain absent until their separate capability and private-data APIs exist. No old portals/operations desk files are targets.

## Verification

At 1440/1100/390/320 in both themes, a real test worker edits a normal field; a separately authenticated authorized Operations test actor sees the same permitted value after refresh. Cross-worker/tenant and revoked-session attempts fail; no private fields appear in DOM, storage, normal network responses or exports. Keyboard, touch, focus, reduced motion, error/loading and no-flash theme behavior pass. Use synthetic test actors only. Run focused tests, lint, TypeScript, Vercel build, browser matrices and fixed-SHA independent review before Kartik acceptance.
