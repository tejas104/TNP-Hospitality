# TNP-CAREERS-WORKSPACE-ASTRA-M5 - public name, themes, worker profile and Operations adoption

Status: DRAFT / REVIEW CAPACITY HOLD. No writer, branch, worktree, port, Ready launch SHA, reviewer appointment, or push authority is active. Current integrated source baseline is main 831597573fe6898f8a19e5cf416d36a364c3544b; reverify after predecessor integrations.

Human owner and exact-SHA acceptance reviewer: Kartik. Required UI author: verified gpt-6-astra at the effort selected by the Ready contract. External Claude source review is required; independent Sol review covers the worker/Operations privacy seam. P issues one exact owned-path lease after review capacity and backend contract are ready.

## Result

1. Rename human-facing "Freelancer" access/workforce language to "Careers With Us" across public navigation, launcher, career information, workspace and relevant Operations labels. Keep /freelancer and internal record keys compatible. Render "Hostess/Welcome Girls" as the user-facing role label through a shared formatter so Planner, worker and Operations views agree.
2. Build a complete task-first worker profile: profile details, portrait, role/skills/availability, verification checklist, payout-onboarding status, and clear editable/submitted/reviewed states. Use server-backed fields only after the authenticated API is accepted; current preview remains labelled synthetic and never asks for real Aadhaar or bank content.
3. Add a user-selectable restrained gradient theme and teal #008080/beige theme throughout the worker workspace. Retain strong text/control contrast, focus, reduced motion, and readable status colors in both themes.
4. Connect the same worker ID/revision to Operations staffing list/detail. Show photo only where consented, verification status only where authorized, and masked payout status only to the authorized capability. A worker edit must appear after Operations refresh through the accepted API, not through a duplicated browser fixture.

## Proposed ownership

- public/access label and role-display components in app/** and components/tnp/**, scoped by exact inventory at Ready.
- app/freelancer/** and components/tnp/portals/freelancer/** for theme/profile screens.
- narrow Operations worker detail/adoption seam after backend API acceptance.
- focused presentation tests, browser matrix, accessibility checks.

No raw identity/bank data in localStorage, sessionStorage, fixtures, screenshots, or logs. No production upload/payout controls before the accepted server/provider gates. Keep public Client no-login, internal Operations access, RSVP boundaries, homepage composition, and React/Vinext/Vite/Nitro.

## Acceptance

At 1440/1100/390/320 widths, verify the public names and role label, both themes, keyboard/touch/reduced motion, profile edit/error/conflict/save/refresh, empty/pending/rejected/verified document states, and authorized Operations projection. Test cross-worker identity changes and denied Operations access with server responses. Run focused tests, lint, nonincremental TypeScript, Vercel build, full public/access regressions and browser checks. One immutable fixed SHA, fresh reviews and Kartik acceptance precede integration.
