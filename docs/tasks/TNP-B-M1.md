# TNP-B-M1 — Astra public/frontend milestone

Status: VISUAL FEEDBACK CORRECTION READY. The fixed SHA `712610e17a0858e54d258b4e843210a169b5357b` is superseded for review/integration by the user's 2026-09-18 screenshot feedback. Reopen only the same verified Astra writer after P supplies the correction LAUNCH_SHA.
Human owner: Anjaneya / H1 for product/content/visual inputs. Sole human fixed-SHA reviewer: Kartik.
Host: Laptop 1 / DESKTOP-DL9FDM7.
Tool/model: Codex gpt-6-astra / high for the homepage motion/3D design milestone, per the user's 2026-09-17 instruction; confirm the actual session/model/effort at Ready.
Branch: `codex/tnp-b-m1`.
Worktree: `D:\TNP-worktrees\TNP-B-M1`.
SOURCE_SHA: `0190c5c2d6adeef6a1465c4825ce0db4e8a22a0b`, the local-main integration baseline plus a merge of homepage candidate `b1dcac83d29a0c26d490f9ede6dad8bc65f0106d` for correction; this is not homepage acceptance.
LAUNCH_SHA: the commit carrying this Ready contract and the matching STATUS/LANES reservation; P supplies its exact full SHA after commit. Initial writer HEAD must equal it.
Dependencies: integrated TNP-FOUND-01, A, D and TNP-SHARED-FIX-01 through local main `1a32c6b`; seeded homepage candidate above. Prompt 1 then Prompt 2 run sequentially under one lease.
Owned paths: `components/tnp/HomeExperience.tsx`; `components/tnp/public/**`; `components/tnp/AppShell.tsx` and `app/globals.css` only for the serialized public-shell/skip-link correction; `app/services/[slug]/page.tsx`; `app/departments/[slug]/page.tsx`; `app/contact/page.tsx`; `data/public-content.ts`; `data/media.ts` only for documented client-supplied replacements.
Mode: local labelled synthetic responsive-web preview.
Resource: port 3102 / `http://localhost:3102`, verified free at Ready; recheck before binding. Dedicated synthetic browser profile.
Risk: Public-facing motion/3D and navigation. Content approval remains separate; reduced-motion, mobile performance, non-WebGL fallback and truthful separation from the synthetic Operations preview are acceptance gates.
Review appointment: fresh external Claude fixed-SHA review after Astra authorship, plus Kartik as sole human reviewer. Independent Sol is added if implementation crosses architecture/security/data boundaries. Anjaneya/client still supply content and visual decisions but are not a second code-review gate.
Sharing: the sole Astra builder may create checked commits and non-force push only `codex/tnp-b-m1` after live remote comparison. No main merge/push or deployment.

## Shared execution rules
This Ready contract becomes executable only for the exact fresh Astra writer P names with the final LAUNCH_SHA. No other session may infer a lease from this file. Stop for a different initial HEAD, dirty worktree, remote movement, occupied port, unexpected process or path outside ownership; never reset, clean, rebase or merge main to make preflight pass.

At activation read AGENTS.md, TNP-START-HERE.md, docs/PRODUCT.md, docs/DESIGN.md, docs/DOMAIN-RULES.md, docs/ARCHITECTURE.md, docs/LAUNCH-PROTOCOL.md, docs/REVIEW-CADENCE.md, both foundation contracts and the referenced phase specifications. This grouped TASK supersedes their separate per-slice launch branches only when Ready. Verify actual host/model/session, clean initial HEAD=LAUNCH_SHA, SOURCE_SHA ancestry, integrated dependency ancestry and the launch three-file allowlist. Preserve unrelated changes; never reset to make a check pass.

Work through Prompt 1 then Prompt 2 within ONE branch/worktree and ONE lease. After Prompt 1, run its applicable checks, commit and non-force push the named branch once authorized by the Ready contract. Send P the checkpoint SHA, then continue Prompt 2 without waiting for formal review. Pause early only for a shared-contract gap, ownership conflict, failing prerequisite or material security/financial issue. No other task, implementation subagent or writer is authorized.

For every mutation capture expectedGeneration at invocation and retain requestKey for retries of that same action. Handle structured errors and STALE_GENERATION without overwriting newer state. Consume shared services; no local competing shared fixtures, adapter bypass, fabricated success or production authority. Local view/draft state may remain in the owned feature directory, with clearly scoped synthetic-only persistence and reset behavior; raise any missing shared contract to P before inventing a cross-portal representation.

Forbidden: PortalPages barrel, PortalHero, PreviewControls implementation, existing portal route glue, other shared components, lib/contracts, lib/demo, lib/services, package/lockfiles, framework/provider/auth/database config, docs/registers, other lane views, unrelated deliverables/tmp. The explicitly listed AppShell/global-CSS public-shell correction and new public routes are the only shared/route exceptions. Preserve portal preview controls inside labelled portal routes while removing them from the public homepage/service/contact experience. Responsive web only. No live provider calls, real personal data, native app or deployment.

## Checks and final handoff
At both checkpoints run npm run lint; npx --no-install tsc --noEmit; node --experimental-strip-types --test tests/preview-contract.test.mjs; npm run build:vercel; git diff --check. Use npm ci for the initial clean install; reuse this worktree's dependencies afterward unless repair is necessary. Never copy another worktree's node_modules. One heavy build per laptop at a time. No npm test/typecheck/E2E script is assumed to exist.

Verify affected routes at 1440x900 and 390x844 with actual keyboard Tab/Enter/Space, visible focus, primary actions, loading/empty/error/success, refresh/reset, correct record identity and no footer obstruction/overflow. Retain the synthetic warning. Record exact SHA, origin, steps, results and screenshots/assertions outside tracked source. Do not equate a screenshot with interaction proof.

Before final push, self-check the whole two-prompt milestone against both acceptance lists and resolve failures in owned paths. Report both checkpoint SHAs, final fixed HEAD, changed paths, exact commands/results, inherited warnings, browser evidence, known gaps and actual model/session/host. Pause the lease. P fetches the named ref, verifies remote/fixed SHA and ownership, then obtains one consolidated milestone review at that fixed commit. Original builder handles consolidated findings, followed by delta/regression re-review. No force-push, main merge/push or deployment.

Foundation is a prerequisite; TNP-I01 remains the later integrated cross-portal browser gate. A lane's completion does not certify other portals or production behavior.

## Prompt 3 — Godly/Origin-inspired homepage art direction correction

Correction SOURCE_SHA: `6ef1f2ceeb5dcc097773ad7e1b8e8f92ab81cb23`. This prompt supersedes that target for integration/review purposes; it does not invalidate the completed service/detail/contact work. Use the same verified `gpt-6-astra`/high writer and modify only `components/tnp/HomeExperience.tsx`, `components/tnp/public/HomeHero.tsx`, `components/tnp/public/PavilionScene.tsx`, `components/tnp/public/Home.module.css`, `components/tnp/public/Public.module.css`, and narrowly scoped new motion helper/test files under `components/tnp/public/**`. All routes, AppShell, global CSS, content data, enquiry code, package files, platform code and other portal paths are frozen. No dependency change is authorized.

Use [Recent/Godly](https://recent.design/) only as a reference for editorial scale, asymmetry, generous whitespace, strong typography, cinematic section pacing and purposeful scroll storytelling. Use [Origin UI](https://www.originkit.dev/) only as a reference for polished micro-motion, shaders/particles and performance-conscious interaction. Do not copy third-party source, paid components, assets or brand treatments; create an original TNP composition within the existing React/R3F/CSS stack.

Replace the checkpoint model with a single original **Celebration Constellation**: a refined luminous canopy/chandelier-pavilion whose central venue form and orbiting guest, planner, vendor and operations nodes make TNP's multi-sided event orchestration legible. It must read as hospitality/event coordination rather than a generic sci-fi orb, dashboard globe, particle toy or literal building render. Use TNP's ink/ivory/brass palette and restrained celebration accents. The still fallback must communicate the same concept. Keep exactly one canvas, cap DPR, use modest geometry/draw calls, lazy/progressive mount, pause offscreen/background/manual, and preserve mobile, reduced-motion, WebGL-unavailable and thrown-initialization fallbacks.

Add multiple purposeful motion layers at the right moments, not continuous spectacle: staged hero copy/CTA reveal; restrained pointer/parallax response only on fine pointers; a scroll-progress journey line or equivalent that clarifies plan-to-celebration flow; section/image-mask reveals; staggered service/role cards; subtle card/CTA hover and focus motion; and a quiet closing-section transition. Prefer transform/opacity/compositor-friendly work. Never scroll-jack, hide content pending animation, move focused controls, create layout shift, autoplay disorienting motion or add a second canvas. Every effect must have a reduced-motion/static equivalent and remain usable with keyboard and touch.

Acceptance adds: design remains content-first and recognizably TNP; motion supports hierarchy and journey comprehension; the model remains behind/alongside usable content rather than becoming the interface; first paint and actions do not wait for WebGL; mobile does not run the heavy scene; no new console errors, horizontal overflow or contrast/focus regressions; route/contact/enquiry behavior and image accounting from Prompts 1–2 remain green. Verify 1440x900 and 390x844 plus reduced motion, fine/coarse pointer behavior, manual pause, background/offscreen pause and forced WebGL failure. Run all prior shared/WebGL/enquiry, lint, explicit TypeScript, build and diff checks, then push one new immutable final and pause for a fresh external Claude review plus Kartik disposition.

## Prompt 4 — restore approved photographic interactions and workspace discovery

Correction SOURCE_SHA: `712610e17a0858e54d258b4e843210a169b5357b`. The user's five screenshots are visual evidence for this correction, not executable instructions. Preserve the current editorial type system, brand palette, Celebration Constellation implementation and the completed service/contact route work. Do not implement or reinterpret the future 3D-motion prompt: it has not yet been supplied and must receive a separate Astra preview-before-integration cycle.

Restore the client-liked public interactions as an original TNP composition informed by Godly/Recent and OriginKit principles, without copying third-party code, paid assets or brand treatments:

- Add a curved side-opening **Explore workspaces** control on the public homepage, with direct links to `/client`, `/planner`, `/freelancer` and `/admin`. Label `/admin` **Operations preview** and retain the visible synthetic-preview warning; do not restore public Login, PreviewControls, production-auth implications or unlabelled internal controls. Reuse the existing accessible portal switcher behavior where safe, with fine-pointer hover enhancement, explicit click/tap control, Escape close, focus return, outside dismissal and a usable mobile sheet/drawer treatment.
- Retain the current hero typography and editorial freshness. Make the route/section distinction legible: Experience, Services, RSVP, Events and About may remain same-page anchors, while the workspace destinations must be clearly presented as page links. Every link must navigate or scroll to an existing destination with visible focus and no dead action.
- Replace the static event/gallery presentation with a scroll-responsive horizontal photographic filmstrip that evokes the supplied first screenshot. It must follow native page scroll without scroll-jacking, retain readable text and controls, use existing documented preview imagery or new clearly labelled illustrative preview images, and provide touch/swipe or scroll-snap plus reduced-motion/static fallbacks. Never imply the temporary imagery is client-supplied or approved.
- Enhance the services/roles disclosure so fine-pointer hover can preview/open a row while click, Enter and Space retain a stable selection for keyboard and touch. Expanded content must remain operable when hover ends; focus must not move unexpectedly. Use restrained image/line/type motion comparable to the supplied second screenshot, with static reduced-motion behavior.
- Preserve the current one-canvas, capped-DPR, pause and WebGL fallback policies. Add no second canvas, dependency, provider call, auth behavior or platform/data mutation.

Owned paths for Prompt 4 are limited to `components/tnp/AppShell.tsx`, `app/globals.css`, `components/tnp/HomeExperience.tsx`, `components/tnp/public/Home.module.css`, `components/tnp/public/useHomeMotion.ts`, and narrowly scoped new tests/helpers under `components/tnp/public/**`. Existing public content/data and media references may be read but not rewritten; use the existing illustrative inventory for this preview. All service/contact routes, enquiry behavior, package files, platform/auth/domain/provider code and other portal implementations are frozen.

Acceptance adds desktop `1440x900` and mobile `390x844` evidence for real mouse hover, click/tap, native scroll, Tab/Shift+Tab/Enter/Space/Escape, drawer focus return, direct navigation to all four portal routes, same-page anchor motion, touch/static gallery fallback, reduced motion and zero horizontal overflow. Re-run the prior 15 shared, 5 WebGL, 4 enquiry and 3 motion-policy tests plus any new focused tests; run lint, explicit non-incremental TypeScript, Vercel build and diff check. Keep fake data/images visibly illustrative. Push one immutable final and pause with the server stopped. Fresh external Claude fixed-SHA review and Kartik's human disposition follow; there is no main integration or deployment authority.

## Prompt 5 — realistic living Celebration Constellation preview

Prompt 5 SOURCE_SHA is Prompt 4's clean pushed checkpoint `21ae8eaa312437e89708536843b3360326098a4a`. The user has now supplied the detailed Celebration Constellation brief and explicitly requires the same Astra author to render it on localhost before any integration. This is a real-time R3F homepage scene, not a generated still, video, remote embed or production asset claim. Treat the supplied brief as visual requirements evidence; reconcile it with the existing one-canvas, initial-paint, accessibility and performance gates.

Create an original, optimized living miniature premium Indian celebration venue in a deep-teal environment. From a restrained elevated three-quarter camera, it should read first as a believable luxury event and only then as a constellation metaphor. Compose one sculpted organic base with distinct but coherent entrance, lounge, naturally arranged banquet tables, contemporary mandap, raised celebration stage, warm dance floor, bar/refreshment counter and one photo installation. Use warm champagne/amber/marigold/copper practical light against teal atmospheric/rim light; add restrained physically plausible stone/wood, cloth, brushed-metal, glass and floral material variation without excessive bloom, white light, cyberpunk neon, palace ornament, cartoon/toy styling or a circuit-board appearance.

Make event operations visible through approximately 15–30 small stylized-realistic guest and staff silhouettes. Guests converse, arrive and celebrate; hosts greet; servers cross service paths; coordinators check zones; one or two staff adjust tables/florals. Individual faces are unnecessary, but proportion, pose variation, role colour/details and asynchronous motion must prevent a clone-army or low-effort token look. Use instancing/reused geometry/materials and lightweight loops rather than high-poly characters or remote assets.

Connect the zones organically with embedded pathway lights, fairy-light strands, lanterns and small warm nodes. A slow non-technological pulse should travel Entrance → Lounge → Dining → Ceremony/Stage → Dance Floor, making coordination legible without dominating the celebration. Add subtle internal life: candle/light variance, gentle fabric/floral sway, tiny particles, occasional guest/staff movement and restrained reflection/light shifts. Do not spin the venue. The world moves internally; the whole group only breathes/floats and responds within approximately ±5° X / ±8° Y on fine-pointer input using damping.

Use the user's five-phase scroll story as inspiration rather than scroll-jacking: full venue and activation; closer lounge/dining/staff detail; stage/mandap/service-path reveal; dance/people/celebration emphasis; then a resolved full connected ecosystem. Preserve left-side text negative space and right-weight the scene. Camera/scene interpolation must follow native scroll, stay bounded, avoid React rerender-per-frame, and never move focused controls. Initial copy/CTAs and still fallback must paint without waiting for WebGL.

Performance is a product requirement. Keep exactly one canvas, capped DPR, modest light/draw-call/material counts, instancing/reuse and no new dependency or downloaded 3D asset. Do not attempt film-quality geometry or costly real-time shadows merely to satisfy the word "realistic"; realism comes from composition, proportion, material response, lighting, depth and purposeful motion. Desktop receives the full optimized scene. Existing mobile, reduced-motion, manual pause, background/offscreen suspension, unavailable/thrown/context-lost WebGL and still-illustration paths remain complete, truthful and visually consistent. No real tracking, operational data or client-supplied asset claim.

Prompt 5 ownership is limited to `components/tnp/public/PavilionScene.tsx`, `components/tnp/public/HomeHero.tsx`, `components/tnp/public/Home.module.css`, `components/tnp/public/useHomeMotion.ts`, `components/tnp/public/motion-policy.ts`, `components/tnp/public/motion-policy.test.mjs`, `components/tnp/public/webgl.ts`, `components/tnp/public/webgl.test.mjs`, and narrowly scoped new scene helper/test files under `components/tnp/public/**`. Prompt 4 drawer/filmstrip/disclosure behavior, AppShell/global CSS, routes, content/media data, enquiry, packages and all platform/portal/provider files are frozen.

Acceptance adds visual evidence at 1440x900 for recognizably distinct venue zones, people/staff operations, warm-vs-teal material/light hierarchy, readable left copy and all five native-scroll scene states; the scene must not resemble the prior abstract canopy, a sci-fi orb, a generic dashboard or a spinning product. Verify fine/coarse pointer, paused/resumed, offscreen/background, live reduced-motion change, mobile still, WebGL unavailable/thrown/context-loss fallback, exactly one canvas, capped DPR, zero new console errors/overflow and no regression in Prompt 4 interactions/routes. Run all 31 Prompt 4 tests plus focused scene-policy tests, lint, explicit non-incremental TypeScript, Vercel build and diff check. Push one immutable final, stop the server/browser and pause. P independently opens the final on localhost for the user before fresh external Claude and Kartik review; no integration, main push or deployment.

## Prompt 6 — abstract constellation art-direction replacement

Status: DEFERRED BY USER until the entire non-3D frontend is implemented. Do not execute this prompt from the existing launch packet. The current earlier constellation remains a temporary preview only; issue a fresh final-scene source/launch after Client/Planner, RSVP, Freelancer, remaining service/maintenance/reporting UI and final responsive polish are complete.

Prompt 5 is rejected by the user before commit/push: the detailed venue, people and object density are the wrong art direction. Discard only the uncommitted Prompt 5 scene work and preserve the clean pushed Prompt 4 checkpoint plus dispatch history. The user wants the earlier abstract Celebration Constellation language restored and elevated—not a literal venue.

Create one original abstract kinetic **Celebration Constellation / Solar Loom** on a clean white-to-warm-ivory background. Start from the conceptual strengths of the earlier brass canopy: a clear central luminous celebration core, elegant architectural arcs/ribs, suspended lights and a small set of coordinated role/orbit nodes. Evolve it into a memorable motion sculpture with flowing champagne-gold light, restrained ruby/red-orange accents and warm white highlights. No humans, banquet tables, building zones, floating island, literal mandap, stage, bar, furniture or dense decorative inventory.

The sculpture should feel premium, spatial and event-related rather than generic: interlaced canopy ribbons suggest hospitality; four distinct orbiting tokens/nodes suggest Client/Planner/Freelancer/Operations; fine light paths gather into the central celebration core; a few suspended lantern/prism elements create depth. Use polished champagne/brushed-gold and translucent warm-glass responses against the light background, with enough soft shadow/contrast to preserve form. Red is an accent pulse/tint, not the dominant field. Avoid black/dark-teal backgrounds, sci-fi HUDs, starfields, particle storms, neon tubes, logo animation, jewelry-product spin or literal technology objects.

Motion design carries the idea: a slow breathing/floating canopy; asynchronous ribbon/light shimmer; quiet pendulum drift; small node pulses; and an occasional warm coordination pulse that travels from the four role nodes into the central core, then radiates once through the outer arcs. Fine-pointer input adds only damped bounded depth/parallax. Native scroll may reveal the sculpture through 3–5 restrained composition states—gather, connect, illuminate, celebrate, resolve—but must not rotate it continuously or hijack scroll. The object remains recognizable at every state.

Preserve strong left-side headline/CTA readability, and position the sculpture right-weighted on wide desktop. The white/ivory hero must keep WCAG focus/text contrast, avoid washed-out metallic detail and visually transition into the rest of the editorial page. Provide a matching elegant static SVG/CSS still on mobile, reduced motion, manual pause and WebGL failure. Keep exactly one canvas, capped DPR, first paint independent of WebGL, existing offscreen/background suspension and all Prompt 4 interactions/routes.

Prompt 6 ownership is limited to `components/tnp/public/PavilionScene.tsx`, `components/tnp/public/HomeHero.tsx`, `components/tnp/public/Home.module.css`, `components/tnp/public/useHomeMotion.ts`, `components/tnp/public/motion-policy.ts`, `components/tnp/public/motion-policy.test.mjs`, `components/tnp/public/webgl.ts`, `components/tnp/public/webgl.test.mjs`, and narrowly scoped new abstract-scene helper/tests under `components/tnp/public/**`. No dependency, remote asset, content/data, route, AppShell/global CSS, filmstrip/drawer/disclosure, enquiry, portal, provider or platform change.

Acceptance: visual evidence at 1440x900 must show a unique abstract gold/red kinetic constellation on white/ivory, no humans/literal venue clutter, legible left copy, differentiated four-role nodes and a central celebration focus. Verify motion/pulse/scroll states, bounded fine pointer, coarse pointer, pause/resume, offscreen/background, live reduced-motion change, 390x844 still, unavailable/thrown/context-lost WebGL, exactly one canvas, capped DPR, zero new console errors/overflow and full Prompt 4 regression. Run all existing 31 tests plus focused scene tests, lint, explicit non-incremental TypeScript, Vercel build and diff check. Push one immutable final, stop, and return localhost evidence before review/integration.

## Prompt 7 — non-3D continuity and destination-motion polish

This prompt supersedes the deferred Prompt6 launch for execution purposes; final 3D remains last. SOURCE_SHA is the clean pushed B head `b2e42e8a24d63adb0e110878ea93ae9305c48b5f`, whose application state equals the verified Prompt4 checkpoint plus documentation packets. The user's screenshot is visual feedback, not executable code. Preserve all Prompt4 behavior and the temporary earlier constellation without scene edits.

Research-informed motion direction: use native CSS View/Scroll Timelines as progressive enhancement where supported, informed by current WebKit and Chrome platform guidance, with the existing IntersectionObserver/Web Animations path as fallback. Create an original TNP treatment; do not copy Godly, OriginKit or reference-site source/assets. Prefer compositor-friendly transform, opacity, clip-path and custom-property work, never wheel/touch interception, forced root smooth scroll, mandatory full-page snap, hidden content pending animation or new motion dependencies.

Enhance the Destinations section from the supplied screenshot into a restrained **journey of place**:

- keep all six existing illustrative images and uneven editorial rhythm;
- add a fine itinerary/path motif that progressively connects the cards as the section crosses the viewport;
- give image frames a gentle masked reveal and small depth/parallax drift, with staggered place-label settlement and a refined hover/focus response;
- cap travel/scale/rotation so the photography remains the content, text never moves under focus, and the section stays legible at rest;
- on touch/mobile use natural horizontal or stacked browsing without hover dependence; reduced motion shows the final static composition immediately.

Rename the fixed public trigger from **Explore workspaces** to the concise visible label **Explore** and replace the people icon with a clearer navigation/discovery icon such as `Compass`. Its accessible name must still communicate that it opens TNP workspace previews. Rename the drawer heading to a more polished phrase such as **Choose your TNP space**; retain the four truthful page links, synthetic warning, hover/click/tap/Escape/outside/focus-return behavior and mobile treatment.

Make the long page feel continuous. Each major public section must declare its current and next surface colors and blend the final approximately 10% vertically into the following section. Teal-to-ivory seams are mandatory; also smooth ivory/soft/teal boundaries so there are no abrupt horizontal color cuts. The blend must be part of the section background (or a pointer-events-none decorative layer), never obscure text/images/focus rings or create bands under transparent content. Keep enough solid color behind content for AA contrast. Mobile and reduced motion retain the same static color continuity.

All current and future non-3D frontend milestones must include purposeful replaceable image slots where imagery materially improves hierarchy or comprehension. Reuse `data/media.ts` or feature-local typed media metadata, keep alt text and provenance/illustrative labels, avoid remote hotlinks and ensure client assets can later replace preview sources without layout/component rewrites. Do not add decorative images to dense operational tables/forms merely to satisfy an image count.

Prompt7 ownership is limited to `components/tnp/HomeExperience.tsx`, `components/tnp/public/Home.module.css`, `components/tnp/public/useHomeMotion.ts`, `components/tnp/public/WorkspaceDrawer.tsx`, `components/tnp/public/workspace-interaction.ts`, `components/tnp/public/workspace-interaction.test.mjs`, and narrowly scoped new local motion helper/tests under `components/tnp/public/**`. Do not edit AppShell/global CSS, 3D scene/hero, routes, data/media inventory, enquiry, packages, portals, platform/providers or project registers.

Acceptance: verify 1440x900 and 390x844 screenshots at destination entry/mid/exit, visual 10% section blends, no opaque seam/overlap, all six images and labels, trigger icon/label and drawer behavior, mouse hover, keyboard focus/activation/Escape, touch, reduced motion, no CSS-timeline support fallback, no overflow or new console/hydration errors. Re-run all existing 31 tests plus focused transition/motion tests, lint, explicit non-incremental TypeScript, Vercel build, source-range diff check and all public route/link regressions. Push one immutable final, stop and return localhost evidence. Fresh Claude/Kartik review still precedes integration.

## Prompt 1
This is the public-site milestone, not permission to change platform/domain behavior. Build F01 readiness within B-01 using `gpt-6-astra`: preserve the strong photography and editorial hierarchy, then deliberately refine motion, transitions and depth. Replace the current pavilion with a materially different single purposeful 3D guest-journey/venue-choreography model—not a recolour—while preserving one-canvas, capped-DPR, offscreen pause, manual pause, reduced-motion, mobile and non-WebGL fallbacks. Resolve the external review findings: AA body-text contrast, 3:1 focus indication on light surfaces, a truthful vendor-interest enquiry action, and a useful first-focus skip link. In the bounded AppShell scope, remove public Login/Operations Demo/PreviewControls/portal-switcher exposure from public homepage/service/contact routes while preserving clearly labelled portal previews on portal routes. Do not add another photo wall; retain roughly 75% of placements with transparent accounting. Make anchors and primary actions work. Inventory missing approved copy/assets as gaps; never invent client approval. Commit/push checkpoint 1 after checks and continue Prompt 2.

## Prompt 2
Complete F02 within B-01: reusable service/department detail pages with valid/invalid slug handling and enquiry entry/receipt through submitEnquiry. Validate synthetic name/email/message, display pending/error/retry and persisted receipt, and replay a repeated request safely without claiming an email was sent. Recheck every homepage link created in Prompt 1 against the final destinations. Use documented content provenance; missing three-service/twelve-department approved copy or TNP imagery prevents content certification even if the preview UI works. Do not fill empty screens just to count them as ready. Run final desktop/mobile/keyboard and shared checks, push final checkpoint and pause.
