# TNP-OPERATIONS-REPORTS-CLAUDE-M2 — Operations Reports & Audit frontend

Status: FIXED-SHA HANDOFF — PAUSED FOR FRESH SOL/HIGH REVIEW. Sole NEW Claude Code desktop session `7785999d-ff07-40fe-b2ae-86893368bf5b`, actual reported `claude-opus-5` on `DESKTOP-DL9FDM7` under `desktop-dl9fdm7\dell`, returned its writer lease at clean pushed remote-equal final `89fec9df168a49b9621ea4adda6c4312dbd5b8dc`. The effort field was unavailable inside the session; no effort value is inferred.

Checkpoint: `0745f339ff70f410190e40adb3b4490c30c441c1`.

Final scope is exactly seven Operations-local files: modified `AdminOperations.tsx`, `AdminOperations.module.css`, `operationsState.ts`; added `OperationsReports.tsx`, `operationsReportsState.ts`, `operationsReports.test.mjs`, `operationsReports.browser-check.js`. Claude reports 15/15 shared, 22/22 Operations, lint with three inherited warnings, explicit TypeScript, build, diff and 41/41 browser checks at 1440/1100/390 PASS. P independently verified remote equality, launch ancestry, exact scope, clean worktree/free port, 37/37 combined tests, explicit non-incremental TypeScript and diff check. Fresh independent Sol/high review and Kartik exact-SHA acceptance remain mandatory; no writer is active.

Responsible human/domain owner and sole human fixed-SHA acceptance reviewer: Kartik / H2.

Source SHA: `2d95fe8c8ad57fc6f583edf83c22e86cc30f4e83` on the local serialized `main` history. Live `origin/main` remains stale at `fe277f29a41e4a9f078da9b86e42e1037e4583bc`; no remote-main publication or production deployment is implied. This same-host task obtains the exact launch through the local verified worktree, not by treating stale remote main as current.

Integrated dependencies verified as ancestors of Source:

- reviewed Operations integration `5b13417149dafc473b6b29412ef3431037060517`;
- reviewed shared correction integration `8eeb56710bba121790bc94bf5e64bbba01b363a9`;
- reviewed foundation application `d8473f888cc70819ff8a149f3544e81a6b6ea36b`.

Launch SHA: the commit carrying this Ready task plus its matching `docs/STATUS.md` and `docs/LANES.md` entries, supplied externally after commit creation. Initial task worktree HEAD must equal that exact SHA. The only `SOURCE_SHA..LAUNCH_SHA` paths are those three files.

Branch/worktree/port:

- `codex/tnp-operations-reports-claude-m2`
- `D:\TNP-worktrees\TNP-OPERATIONS-REPORTS-CLAUDE-M2`
- `3109` / `http://localhost:3109`

Preflight: branch and worktree were absent and port3109 had no listener before this Ready commit. P creates/verifies the isolated worktree at Launch after commit creation. Claude must never edit the base clone or read/use its unrelated untracked `tmp/` directory.

Exclusive writer lease: reserved for only session `7785999d-ff07-40fe-b2ae-86893368bf5b` after P supplies the activation message and Claude verifies the exact worktree/branch/HEAD/ancestry/cleanliness/port. No previous TNP Claude session, reviewer session, subagent or other writer may use this worktree. Lease ends at fixed-SHA handoff or P revocation.

Review sharing: this Claude writer may create checked commits and ordinary non-force push only `codex/tnp-operations-reports-claude-m2`. It may not force-push, move a reviewed SHA, merge/rebase main, deploy, publish, or change providers. P may fetch/inspect and controls later integration.

Review appointment: fresh independent Codex `gpt-5.6-sol` / high fixed-SHA review in a clean detached worktree after final, focused on identity, reporting, export and any financial-status semantics. Kartik separately accepts the exact SHA. Author self-review does not satisfy either gate.

Mode/risk: labelled synthetic responsive-web frontend. Operational reporting and audit presentation are medium product complexity; any finance status is read-only preview evidence, not profitability, tax, ledger, reconciliation or payment authority. No production authorization, immutable report archive, scheduled/email delivery, provider action, live tracking or official export claim.

## Result

Replace the disabled Operations **Reports** destination with a polished, responsive, accessible, service-backed Reports & Audit workspace. It must extend the existing client-liked Operations composition and derive every displayed/exported value from frozen `PreviewService` queries and stable record identities. It must remain truthful under filtering, selection, reset, async races, empty/error states and browser export failure.

The complete product, visual, state, adversarial-test, browser and handoff specification is `docs/dispatch/2026-09-19/OPERATIONS-REPORTS-CLAUDE-NEW-SESSION.md`. Its startup-only stop has now been satisfied by the accepted preflight; this Ready contract plus P's activation message authorizes the implementation portion. Where the packet and this contract differ, this Ready contract controls exact source/launch/lease coordinates.

## Writable paths

Only `components/tnp/portals/operations/**`, including Operations-local components, CSS, pure view-model helpers, tests, browser checks and untracked evidence that the task deliberately adds there.

Expected bounded shape:

- expose Reports through the existing desktop and compact section systems;
- add a focused `OperationsReports` surface rather than making `AdminOperations.tsx` a second monolith;
- derive staffing coverage, attendance/verification exceptions and audit explorer data from typed service records;
- add exact-current-view browser CSV only if it stays dependency-free and truthful;
- keep stable IDs visible/available for duplicate-looking records;
- use short purposeful motion with genuine reduced-motion parity.

## Frozen paths and stop conditions

Freeze all `app/**`, shared shell/global CSS/components, `lib/contracts/**`, `lib/services/**`, `lib/demo/**`, shared fixtures/tests, other portal/public files, package/lock/config/framework/hosting files, docs/registers from the feature branch, server/API/auth/database/provider code and real personal/production data.

Do not invent missing queries, fixtures, business ledgers, report history, official/scheduled delivery, maintenance CRUD, auth, tax/profitability or payment success. If the requested surface needs a frozen contract change, stop and report the exact prerequisite. Ephemeral component state may hold filters/selections only; it cannot impersonate persisted business state.

## Acceptance gates

- Reports is reachable in desktop and compact navigation with correct `aria-current`, focus transfer and no stale detail.
- Staffing coverage joins by exact event/position/assignment IDs and keeps similar-looking functions distinct.
- Attendance states preserve recorded, GPS missing, GPS denied and outside-radius distinctions.
- Audit filters preserve actor/entity/action/entry identity even for duplicate-looking text.
- Current snapshot/generation and synthetic limitations are explicit.
- Loading, scenario-empty, filtered-empty, error/retry, reset and late-result races are handled without obsolete counts, selection, export or success feedback.
- CSV, if included, uses the same typed current filtered view model, correct quoting/deterministic columns/stable IDs, exact visible-row inclusion, failure feedback and URL cleanup.
- No profit, tax, balance, payment/provider success or official immutable-report inference.
- 1440x900, compact 1100x900 and 390x844 behavior; keyboard/touch-equivalent controls; AA contrast; visible focus; semantic/live-region restraint; no page overflow or clipped targets; reduced-motion parity.
- Pure helper, React/source-policy and real-browser behavior are all exercised. Screenshots alone do not prove interaction.

## Required checks and handoff

Run the exact focused Operations tests plus `tests/preview-contract.test.mjs`, lint, explicit non-incremental TypeScript, Vercel build and diff check. Do not claim nonexistent npm test/typecheck/E2E scripts. One unchanged Windows Nitro `EBUSY` retry is permitted only after stopping task-owned processes.

Create a checked checkpoint after pure report/view-model behavior, then one immutable final after UI/browser verification. Commit only owned paths; never `git add .`. Stop task-owned server/browser, prove port3109 free, clean local/remote equality and return actual identity, Source/Launch/checkpoint/final SHAs, changed paths, exact command results, browser assertions, CSV-visible-ID comparison, limitations and synthetic-data confirmation. Then pause for fresh Sol/high review and Kartik acceptance. No main merge/push, deployment or provider mutation.
