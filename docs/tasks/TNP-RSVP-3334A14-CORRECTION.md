SESSION TYPE: NEW SESSION

# TNP RSVP fixed-SHA correction

Status: **Ready reservation**. This metadata-only commit carries the Ready contract; P supplies its full LAUNCH_SHA after commit. The writer must verify its new correction worktree starts exactly at that SHA before the first source action. Reusing the interrupted review subagent or outgoing author session is forbidden.

Writer: new Codex task `01a0d4c0-0adf-7850-a94b-75dc268f2bfc`, actual `gpt-6-sol` / `medium` from its own 2026-09-25 session JSONL `turn_context`, on DESKTOP-DL9FDM7. Its read-only managed preflight checkout was clean at `279e5d5125e757e13a39f437743f774ccb14d935`; it is **not** the writable checkout. Responsible human/operator is Kartik. P reserves the sole RSVP correction writer lease for this exact task in `D:\TNP-worktrees\TNP-RSVP-CORRECTION-20260925` on `codex/tnp-rsvp-correction-20260925` from P's full LAUNCH_SHA dispatch until fixed-SHA handoff or explicit revocation. No other writer, shared-path owner, or server is assigned to that worktree. Port 3135 was free at Ready preflight and is reserved for this task only after the writer rechecks it; the previous 3134 demo must not be stopped.

Review appointment: independent Sol reviewer task `01a0d4b5-1121-7ea2-ae03-74be4015fb1d`, actual `gpt-6-sol` / `medium`, DESKTOP-DL9FDM7, will receive a fresh read-only focused successor turn in a separate clean detached D: review worktree at the final SHA. A separate fresh Claude review of Codex-authored security work is appointed for a Kartik-operated team session with exact identity/model/worktree to be verified before that review starts; no Claude verdict is claimed now. Kartik is the sole human fixed-SHA acceptance reviewer. No reviewer shares the author checkout.

Review-sharing entry: none is authorized for the candidate branch at launch; reviewer can access a local fixed commit by detached worktree. Any later ordinary non-force push of `codex/tnp-rsvp-correction-20260925` requires a separate exact-SHA sharing entry. P is the metadata-only main pusher under the user's current instruction to create the new task and proceed; no application source, provider or deployment push is authorized.

## Objective and immutable input

- Correct the four findings in `docs/reviews/TNP-RSVP-3334A14-SOL-REVIEW.md` against exact local candidate `3334a14dbb600fa09387734bbadbea378e5560b8` without expanding the product. This is the same Kartik RSVP review queue, not an unrelated new milestone.
- SOURCE_SHA is freshly verified local integrated main `279e5d5125e757e13a39f437743f774ccb14d935`, with documentation-only commits ahead of live `origin/main` `10f149472c06fa8ad7855bf24f21e4fa9a41c94f`. The later three-file Ready commit is LAUNCH_SHA; the writer's initial D: worktree HEAD must equal its full hash and SOURCE_SHA must be an ancestor. The immutable RSVP candidate is **correction input**, not an approved or integrated main dependency. Merge it only into the isolated correction branch as the first authorized source action, record both parents, and review the entire aggregate before any source integration. `git merge-tree --write-tree 279e5d5 3334a14` returned clean tree `1e82e6e2e184f2f06716efc21ebe964844244f9d`; stop if the real merge differs or conflicts.
- Branch/worktree `codex/tnp-rsvp-correction-20260925` / `D:\TNP-worktrees\TNP-RSVP-CORRECTION-20260925` were absent at Ready preflight. P creates them at LAUNCH_SHA after sharing main. The writer verifies exact path, branch, HEAD, source ancestry, clean status, model/host, lease and port before edits. This work is a bounded correction of the rejected RSVP candidate within the same Kartik review queue, so it does not consume a new unrelated human work assignment.

## Bounded correction scope

1. **Close/reply race (P1):** make event closure and any new pending reply/information write conflict or serialize on a common event revision inside Mongo transactions. Add a deterministic interleaving test and, if a configured development MongoDB is available, a real transaction race check. Never claim a fake store proves Mongo concurrency.
2. **Shared-contact suppression (P1):** choose and document conservative event-scoped contact policy. An opt-out, wrong-number or block must prevent message preparation and audience eligibility through every party sharing that contact, including newly imported parties. Preserve separate party/person RSVP state. Address legacy rows without `contactKey`; add indexes/migration notes before production. Reconsent requires fresh reviewed evidence.
3. **Consent evidence (P2):** require an appropriately reviewed reply before recording consent, including after opt-out. Keep original reply text unchanged and test pending/confirmed, wrong party, stale after opt-out and suppressed contact cases.
4. **Replay authorization (P2):** recheck current actor, entitlement and event scope before returning a stored RSVP mutation response. Contain shared helper changes so other platform mutations retain their behavior; test suspended/revoked entitlement, removed event membership and valid replay.

Owned source paths after the immutable candidate merge: `server/rsvp/**`, `app/api/v1/rsvp/**` only if route contract must change, `server/idempotency/service.ts` only for an explicit narrow RSVP replay hook, `tests/rsvp-service.test.mjs`, new `tests/rsvp-mongo-race.test.mjs`, `tests/platform-foundation.test.mjs` only for a generic-helper non-regression assertion, and `docs/rsvp/CONTACT-PERMISSION-POLICY-2026-09-25.md` for the event-scoped policy and migration/retention note. No other server/data/shared file is owned; P must serialize an exact amendment before one is edited. Frozen: public/homepage, Planner, Careers, Admin/Operations, RSVP browser UI, shared auth/session/tenancy, package/deployment/provider configuration, `docs/STATUS.md`, `docs/LANES.md`, and preserved `.claude/`, `output/`, `tmp/` in main.

## Checks and return

Run focused tests, every tracked `*.test.mjs`, lint, non-incremental TypeScript, Vercel build and diff check. Record inherited warnings and environmental failures honestly. Verify `provider_disabled`, tenant/event isolation, exact source/changed paths and clean final SHA. Return fixed-SHA report with changed files, commands/results, Mongo evidence or explicit absence, limitations, reviewer handoff and current port/process state. No self-certification, main merge/push, provider enablement or deployment.
