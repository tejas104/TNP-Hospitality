# D — TNP-D-M1 correction batch

Give this whole packet to the existing Dev D task; execute both prompts sequentially, with one final review.

## Same-session resume authority

READY again 2026-09-17 for the original sole writer `01a0aded-481f-7121-a788-b8c307f8ab59` after the user confirmed Codex capacity returned. Resume this OLD SESSION only; do not create or substitute another task. The remote correction baseline is still `9785bd5631a99e5372dbca928f62eda52c004384`.

Fetch and read current canonical metadata without merging it into the feature branch. Before editing, report the actual local HEAD, branch, short status, changed/untracked paths, remote equality/ancestry and port/process state. Preserve any interrupted same-session changes. Stop only for an unexpected branch/divergence, another active writer/process, changes outside owned Operations paths, or another material ownership/safety issue. If the preflight passes, continue both prompts below without waiting for another dispatcher response, push checked checkpoints and pause at the final fixed-SHA handoff.

## Interruption hold

PAUSED 2026-09-17 after the user-reported Codex usage interruption. Live remote `origin/codex/tnp-d-m1` remains at correction baseline `9785bd5631a99e5372dbca928f62eda52c004384`; no correction checkpoint is remotely published. This packet does not authorize a second writer while the existing lease remains unresolved.

Preferred path: the same task/session resumes the same worktree when available, first reporting current local HEAD/status and any uncommitted paths. Urgent replacement path: a Claude or replacement Codex task performs `D-TAKEOVER-PREFLIGHT.md` read-only, then waits for P to publish an explicit reassignment that revokes `01a0aded-481f-7121-a788-b8c307f8ab59`. Preserve any local work; do not reset, clean, recreate, rebase or merge main. Reviewer assignment must be recalculated from the actual replacement author.

## Execution contract

READY upon publication with matching STATUS/LANES. Sole writer `01a0aded-481f-7121-a788-b8c307f8ab59`, Kartik, DESKTOP-VO8G3GR, assigned gpt-5.6-sol / high. Base clone is `D:\TNP-Hospitality` on Kartik's computer; implementation worktree is `D:\TNP-worktrees\TNP-D-M1`, branch `codex/tnp-d-m1`. Correction baseline `9785bd5631a99e5372dbca928f62eda52c004384`; retain original launch `58c324953e0f2da775a77efb172059bb84534ae7` as ancestor. Reuse the existing worktree, not a C-drive copy. Port 3104, one heavy build per laptop.

Fetch origin and read this published packet/registers via git show without merging main into source. Read AGENTS, startup/product/design/domain/architecture/launch/cadence docs, original D-M1 and its contracts. Verify process CODEX_THREAD_ID (SESSION_ID corroborates), host, clean exact baseline HEAD, branch, remote and ancestry. Browser identity is corroborating only. Report observable model metadata honestly; absence is not mismatch. Stop on a real identity/ownership discrepancy without removing work. No reset/recreation/rebase.

Own only `components/tnp/portals/operations/**`, including local tests/helpers/styles. Shared contracts, services, fixtures, AppShell, dependency files, registers and other portals remain frozen. Synthetic preview only; no finance or D-03 expansion. Checked commits and non-force pushes to this feature branch are authorized. No main write/merge or deployment. Lease expires at final handoff.

Read `docs/reviews/TNP-D-M1-sol-review.md` in full and combine any already-available Sonnet findings into this pass; do not claim unseen reports accepted. No new unchanged-SHA review is needed before fixing the four recorded findings.

## Prompt 1 — correct identities and decision eligibility

Remove invented/hard-coded selected IDs. Derive and reconcile every action's selection from its actual current options after load/reset/mutation. Attendance capture must list only contract-supported actionable confirmed assignments; initial reset has no confirmed assignment awaiting capture, so show a clear prerequisite/empty state. After valid admin assignment, make the new actionable record selectable/current. Disable any submission whose selected ID is absent. After replacement, remove/reconcile replaced assignment IDs.

Keep filtered event list, selected event detail, positions and roster aligned. When filtering excludes selection, reselect a visible event and load its matching roster before actions, or clear detail. No-match must not retain another event's detail/action controls. Guard delayed roster responses against overriding a newer selection.

Restrict ordinary verification decisions to valid pending applications with resolvable worker identity and valid role/evidence. Reviewed and invalid applications are not decision options; show reviewed outcomes read-only and clear empty pending state. Do not invent a re-review operation. Preserve required human reasons.

Verify reset, changing filters/no matches, two-event switching, post-assignment and replacement selection, pending-to-reviewed, invalid application exclusion and empty pending queue. Run original milestone checkpoint checks, commit/non-force push checkpoint 1, then continue.

## Prompt 2 — retry identity and complete regression

Bind feedback to its exact action/record/request identity and retryability. Clear or replace old retained retry state before attendance pass lookup. A displayed attendance error must never execute a retained replacement. Only display retry when a matching retained transient request or composite attendance action can actually run. Deterministic validation/rejection outcomes need correction/recovery rather than same-key replay buttons. Preserve request keys for genuine same-action retries and capture generation at invocation.

Reproduce replacement INELIGIBLE followed by attendance preflight failure: no wrong-action retry. Verify first-action attendance failure has no inert retry. Exercise stale generation, delayed results, reset during pending action and successful refresh. Recheck full/ineligible/overlap rejection preserves original assignment, valid replacement refreshes roster/audit, evidence-preserving attendance correction, processing/approved ledger rejection unchanged, nonresponse and linked pass capture.

Final checks: lint, `npx --no-install tsc --noEmit`, `node --experimental-strip-types --test tests/preview-contract.test.mjs`, build:vercel, git diff --check and focused local tests/probes for these UI defects. Reuse healthy dependencies. Verify desktop 1440x900/mobile 390x844, keyboard and visible focus, all meaningful state/retry/empty transitions, no overflow. Stop preview before build if locks occur. Record inherited warnings and limitations.

Commit/non-force push final checkpoint; return both SHAs, exact clean remote-equal final SHA, changed paths, all four dispositions, commands and browser evidence. Pause for one focused consolidated independent Sol and separate Sonnet delta/regression review. Anjaneya is human reviewer with Kartik domain input. C's Sonnet developer is not automatically the independent reviewer.
