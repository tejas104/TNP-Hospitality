# C — freelancer application and opportunity milestone

Status: READ-ONLY PREFLIGHT NOW; implementation remains DRAFT until actual Sonnet writer identity, capacity and Ready launch are published. C is the developer, not a reviewer of its own work.

## Startup preflight

Read current origin/main through fetch/git show, AGENTS, startup/product/design/domain/architecture/launch/cadence docs, TNP-C-M1 and its shared contracts. Return actual Claude task/session identity from the available trusted runtime, host, selected/observable model/effort, current path/branch/HEAD and cleanliness. Do not substitute a Codex ID or browser identifier for an unavailable Claude writer identity. Do not invent metadata or create/reset a worktree. List exact missing contract requirements and prepare the acceptance matrix read-only while D completes its correction batch.

Once P publishes Ready with exact source/launch SHA and your identity, execute both prompts below sequentially. Proposed worktree `D:\TNP-worktrees\TNP-C-M1`, branch `codex/tnp-c-m1`, port 3103 on Kartik's laptop. These are proposals, not a current reservation. Own only `components/tnp/portals/freelancer/**` with local helpers/styles/tests. No Operations files, shared fixtures/services/contracts, dependencies, route glue or registers. Label all data synthetic; no real KYC documents, bank data or location permission claims.

## Prompt 1 — application and assessment

Implement the F07 editable application and assessment flow using `registerApplicant` and `submitAssessment`. Provide clear steps, role/field validation, saved local draft behavior, pending verification, assessment outcome, application reference and working error/recovery states. Persist the logical request identity and receipt across reload so unchanged resubmission cannot create duplicates; changed payload starts a new action. Reset invalidates old generation receipts and retries while preserving only documented draft fields. Never portray synthetic assessment as real verification or employment approval.

Verify invalid/valid inputs, partial draft/reload, duplicate submit/retry, reset/stale generation, empty/error/loading and responsive keyboard operation. Run Ready checkpoint checks, commit/non-force push checkpoint 1, then continue without formal review.

## Prompt 2 — opportunities, claim and response

Implement F08 opportunity feed/detail and F09 claim, Coming/Not Coming and briefing flows through the shared service. Provide role/location filters, schedule/timezone, pay units, reporting details and correct two-record identity. Keep claim and attendance response as distinct actions. Display service-backed quantities such as 6/40; do not calculate competing capacity/eligibility rules locally.

Handle full, ineligible and overlapping opportunities, duplicate claims, retained-request retry, expired/stale generation, nonresponse, inactive/replaced assignments and refreshed assignment/roster state. An excluded or empty selection cannot submit a hidden ID. Do not include later F10/F11 attendance/payout/ratings scope or production background location.

Run all final Ready checks: lint, typecheck, contract tests, build, diff and targeted flow tests. Browser evidence at 1440x900 and 390x844 must cover keyboard focus, filters/no matches, successful claim and response, rejected full/ineligible/overlap actions with state preserved, refresh/reset and correct record identity. Commit/non-force push checkpoint 2 only as authorized by Ready; report both SHAs, final clean remote match, paths, commands, evidence and gaps. Pause for one consolidated fresh independent Sol/high review and Anjaneya human review with Kartik domain input. Do not request a self-review from the authoring Sonnet session. No main merge or deployment.
