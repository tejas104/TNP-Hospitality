# Replacement architect P — handoff prompt

You are the replacement TNP architect and delivery dispatcher, continuing an existing project. The user explicitly requested this handoff and next packets for all developers. Do not restart discovery or reissue completed foundation tasks. Repository evidence and latest user decisions govern. Begin read-only; identify your actual writer task/session and publish a serialized dispatcher identity handover before changing shared records. This handoff does not itself invent your identity or grant concurrent ownership with the outgoing P.

## First actions

1. Confirm actual host, process task/session identity, cwd, git common directory, remote, branch, HEAD and status. On Anjaneya's laptop base is `D:\TNP Hospitality`; on Kartik's laptop it is `D:\TNP-Hospitality`. These are separate hosts/clones, not a reason to move the project to C:. Use exact absolute worktree paths. Do not reset/delete/recreate to solve a mismatch.
2. Fetch origin. Read AGENTS.md, TNP-START-HERE.md, docs/PRODUCT.md, docs/DESIGN.md, docs/DOMAIN-RULES.md, docs/ARCHITECTURE.md, docs/STATUS.md, docs/LANES.md, docs/LAUNCH-PROTOCOL.md and docs/REVIEW-CADENCE.md from current origin/main. Read assigned TASKs and referenced contracts. The base checkout may be stale: outgoing base main was 00430c7, while metadata main advanced. Use git show instead of merging metadata into implementation branches.
3. Read `docs/dispatch/2026-09-17/{A,B,C,D}.md` and review reports. Verify all moving refs and exact source SHAs below. Do not treat old review reports as covering new commits.
4. Confirm outgoing P has stopped writing. Outgoing identity is `01a0abe3-6edb-7101-94d8-dc9bd66300d5`, DESKTOP-DL9FDM7. Register your actual identity through one serialized metadata handover; preserve existing developer leases. No additional user confirmation is needed merely to perform the handoff they explicitly requested, but do not self-claim an overlapping active lease.

## Current source and ownership

- Foundation approved/integrated source: `d8473f888cc70819ff8a149f3544e81a6b6ea36b`; integrated foundation merge `e6025cb0d16c433c0d29745d3eea1f9618718a67`. The old mixed-worker finding against 1402026e is historical, not a reason to reopen the same unchanged foundation. Worker projections distinguish undefined Operations from empty worker filter, deduplicate earning IDs and preserve stored Operations batches.
- A candidate `1cd1209f8cf3a26336cb839a4b8d2e6adb6c6e12`, branch `codex/tnp-a-m1`, worktree `D:\TNP-worktrees\TNP-A-M1`. Writer `01a0aa5f-a826-7ab1-9cb7-a44736047ebe`, Anjaneya, DESKTOP-DL9FDM7, assigned Sol/high. Six Sol findings and relayed Sonnet findings require correction. Published A packet resumes this exact baseline for two sequential prompts; check whether the builder has since advanced it. Own client/planner directories only; port3101.
- D candidate `9785bd5631a99e5372dbca928f62eda52c004384`, branch `codex/tnp-d-m1`, worktree `D:\TNP-worktrees\TNP-D-M1` on DESKTOP-VO8G3GR. Writer `01a0aded-481f-7121-a788-b8c307f8ab59`, Kartik, assigned Sol/high. Four Sol findings require correction; published D packet resumes this exact baseline. Own Operations directory only; port3104. Earlier IDs 01a0ac56 and 01a0aa6d are revoked, not fallback identities.
- Homepage B occupied by outgoing P: branch `codex/tnp-home-redesign`, worktree `D:\TNP-worktrees\TNP-HOME-REDESIGN`, source baseline `1b38bd90bd4c42115a20d304d0494b3480e2045c`, launch `1700b2521145c923c6dc31f3f1afe184b27dfd34`. Checkpoints `1f70539303843c23bda4f269bf7a1fe0dc1f40bc`, `2af5641aa5be2c3cb278145df2c0496b8f4344b4`; correction `b1dcac83d29a0c26d490f9ede6dad8bc65f0106d`. Clean and remote-equal at handoff. User authorized correction publication in metadata `a3e3475` and branch push. Sol's two findings were WebGL2 initialization fallback and four small-text contrast failures. Correction adds capability preflight +5 tests and darker text. Follow the latest review report for delta verdict; do not presume PASS.
- B next service/contact packet is read-only preflight; actual B task identity not supplied. C is Sonnet developer, also missing actual Ready identity/launch. C next scope application/assessment + opportunities/claim/Coming, no attendance/payout expansion. Both implementation tasks remain Draft. The user said proceed but supplied no identity; do not invent it. Read-only preflight can collect it without blocking A/D corrections.

## Reviews and cadence

Give each developer the whole lane packet, not a review after every substep. Two compatible prompts, checked checkpoint/push, continue, then final fixed-SHA review. Fetch developer pushes yourself where access permits; do not make the user relay everything. Consolidate all available findings once, original author fixes, then focused delta/regression re-review. No promise that one review can eliminate every defect.

Normally two builders, one complex task per human; A/D corrections are the next pair after P's homepage writer pauses. B/C preparation is read-only, not extra implementation. No broad exception to queue/capacity rules. Opposite-model review remains; sensitive architecture/security/concurrency/finance changes require fresh independent Sol. C's authoring Sonnet is not automatically the independent Sonnet reviewer. Humans: Kartik reviews A/B; Anjaneya reviews C/D, with domain owner input.

Outgoing read-only reviewers: /root/a_m1_review, /root/d_m1_review, /root/home_review. A replacement task may not inherit callable agent handles; use committed reports and fresh independent reviewer appointments when necessary, not assumed access. A/D full Sonnet reports have not been received by P; retrieve existing reports from Kartik rather than commissioning another identical baseline audit. Independent passes, human acceptance and integration are separate facts.

Shared gaps outside lane ownership: PreviewControls reuses a request key after reload with different variant payload; planner requirement service lacks event.bookingId equality enforcement. Prepare a small serialized shared correction task before claiming integrated acceptance. Do not let A/D edit frozen shared paths or hide the gaps with UI-only claims.

## Homepage evidence and limits

New homepage keeps 38 of 51 photographic placements (74.5%; original total includes eight 3D photo textures). It retains20 unique image URLs, so this is not a claim of25% fewer unique downloads. Split editorial hero, geometric3D pavilion, services accordion, two RSVP offerings, compact event/gallery/destination/team content. No framework/provider/dependency migration.

At correction SHA: lint0 errors/3 inherited React compiler warnings; typecheck PASS;13 preview contract +5 WebGL guard tests PASS; build:vercel PASS with inherited non-failing bundle/dynamic-import/trace advisories; diff check PASS. Earlier desktop1440x900/mobile390x844 browser evidence covered no overflow, links, services, gallery, manual pause/resume, offscreen suspension and clean fresh-page console. Browser forcing of WebGL creation failure/OS reduced motion/background visibility and GPU performance were not demonstrated. Guard tests exercise failures through injected canvas creation, not a real browser GPU failure. A second renderer context can still fail after a successful capability probe; do not claim exhaustive GPU recovery. Follow final handoff for any later browser evidence. Source work is not deployed or merged to application main.

## Product direction to preserve

RSVP is a priority service with both TNP-managed client portals and vendor businesses managing multiple events/guests; TNP administers vendor access, membership, enable/suspend and limits. Plan tenant-isolated roles and data, invited/replied/not-replied/coming/not-coming categories, parties/plus-ones, transport/self-drive/pickup, accommodation, guest forms, private WhatsApp attachments, Excel/PDF imports/exports, consent, audit and reliable message jobs. Read RSVP-SERVICE-BLUEPRINT for current scope. Do not implement production tenancy or provider calls inside a public marketing task. Verify WhatsApp approval timelines with current official Meta sources when asked; provide planning buffer, never guarantee approval/delivery or claim a provider submission occurred.

Website direction: keep75% of images, remove about25%, deliberate3D and consistent frontend. Homepage is already implemented on its feature branch; next B work must extend detail/contact pages, not repeat redesign. Preserve existing service/Operations brand structure while improving usability. Avalon is unrelated; inventory any remaining references and remove only proven unrelated project content within a specifically owned cleanup, never broad-delete user deliverables.

## Execution constraints

Do not merge application branches, deploy, change production or providers without explicit authorization for that action. User authorized metadata publication and checked branch corrections, not an application release. Keep secrets and real guest documents out of source/logs. npm ci only if needed; existing commands lint, tsc --noEmit, contract node test, build:vercel. Do not invent npm test/E2E scripts. Stop dev server before retrying Windows EBUSY; one heavy build per host. Read-only reviews and local fixes should not generate redundant confirmation loops.

Metadata worktree was `D:\TNP-worktrees\TNP-FOUND-LAUNCH`, branch `codex/tnp-foundation-launch`; reuse only after confirming clean current state and dispatcher handover. Base has untracked tmp evidence; do not delete it. External worktree ACLs may require elevated exact-path commands; never change ACLs or assume a failed sandbox read means files are absent. Stage exact paths only, non-force pushes only.

Finish startup with a concise current-state summary, then continue the existing packets and review/integration preparation. Do not issue new A/B/C/D feature launches merely because labels exist; concrete Ready leases and dependencies must match.
