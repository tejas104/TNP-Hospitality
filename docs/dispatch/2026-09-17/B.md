# B — next public service/contact milestone

Status: READ-ONLY PREFLIGHT NOW; implementation remains DRAFT pending actual B writer identity, capacity and a published Ready launch. P currently owns the homepage branch. This packet does not authorize a second homepage writer.

## Prompt 1 — prepare the next frontend slice

Read current origin/main via fetch/git show, AGENTS, TNP-START-HERE, PRODUCT, DESIGN, REVIEW-CADENCE, TNP-B-M1, TNP-HOME-REDESIGN and RSVP-SERVICE-BLUEPRINT. Report actual task/session ID, host, selected/observable model, existing directory/branch/HEAD and worktree status. Do not create a worktree, write source or claim a lease yet.

Audit service/department route and content needs against the current shared contract. Prepare the concrete inventory for service details, department details and contact enquiry: valid slugs, unknown-slug handling, titles, descriptions, CTAs, approved versus placeholder copy and assets. Preserve the newly built homepage direction: keep roughly 75% of original image placements, calm hierarchy and its 3D feature. Do not rebuild the old homepage or add another photo wall. Flag any homepage link needing a serialized handoff to P.

For RSVP copy, explain two offerings accurately: TNP-managed service with client portal; vendor accounts with their own events and guests, administered by TNP. Vendor login, WhatsApp automation, private documents and exports remain planned until implemented. Do not imply live login, delivered WhatsApp messages or approved provider setup. Return a proposed exact path list, contract gaps and test matrix. No commit/push is required for this read-only preflight.

## Prompt 2 — execute after the published Ready launch

Run only after P publishes your actual identity, immutable source and launch SHA, merged dependencies, exact owned paths, worktree, port and review appointment. Proposed branch/worktree: `codex/tnp-b-m1` / `D:\TNP-worktrees\TNP-B-M1`, port 3102 on Anjaneya's laptop. Never assume these resources are free while P owns the homepage.

Within the eventual Ready contract, implement service/department detail routes with clear navigation, valid/invalid slug states, accessible responsive layout and local content data. Check and commit checkpoint 1; continue without review. Then implement contact enquiry using typed `submitEnquiry`, field validation, loading/error/retry, generation-scoped durable logical request identity across reload, and a truthful synthetic receipt. Never say email/WhatsApp sent when only preview persistence occurred. Check and commit checkpoint 2, then one consolidated review.

Proposed ownership is new `app/services/[slug]/page.tsx`, `app/departments/[slug]/page.tsx`, `app/contact/page.tsx`, `data/public-content.ts` and explicitly named new local public components/styles. Existing homepage, shared shell/service/contracts, routes outside this list, package/provider/auth files and registers remain frozen unless the published task explicitly assigns a serialized handoff.

At each authorized checkpoint follow the Ready check list; final evidence includes lint/typecheck/contract tests/build/diff, desktop/mobile/keyboard, valid and unknown routes, validation, failed/retried/reloaded enquiry with no duplicates, reset/stale generation and truthful receipt. Fresh independent Sol and separate Sonnet review, Kartik human review and Anjaneya content acceptance remain. No live provider call, production auth, main merge or deployment.
