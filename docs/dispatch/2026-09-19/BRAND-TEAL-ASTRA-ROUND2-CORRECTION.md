SESSION TYPE: OLD SESSION `/root/brand_teal_astra_correction`

DO NOT OPEN A NEW SESSION. Resume only the exact Astra/low collaboration task that completed the clean read-only preflight on `DESKTOP-DL9FDM7`.

# TNP-BRAND-TEAL-M1 — accessibility and complete-palette correction

IMPLEMENTATION CORRECTION ONLY. Start from exact clean remote-equal candidate `fc7746e06601ca670b09dc8dd47895c543dfec58` on:

- branch `codex/tnp-brand-teal-m1`;
- worktree `D:\TNP-worktrees\TNP-BRAND-TEAL-M1`;
- port 3111 only if browser verification needs it and it is free;
- fresh Claude disposition: `CHANGES REQUESTED`.

The orchestrator selected `gpt-6-astra` / low. The child environment exposes no independent model/effort variable; report that limitation rather than fabricating evidence. Before editing, re-prove PWD, branch, HEAD, live remote equality, cleanliness and port state. Stop on any mismatch. Do not inspect, stage or alter the base checkout's unrelated `output/` or `tmp/`.

## Required corrections

1. Remove remaining former dark-green brand colors from the live homepage 3D materials and mobile/reduced-motion/non-WebGL fallbacks. Use the accepted teal family or neutral shadow/overlay where contrast requires it.
2. Restore visible WCAG-compliant text, labels, status pills, errors, boundaries and keyboard focus across public, Client, Planner and Operations teal surfaces. Champagne is decorative/accent only where it fails text contrast.
3. Ensure primary/secondary actions retain a clear resting boundary and visible focus without relying on a cascade-sensitive transparent border.
4. Replace the dark-green inline `PreviewControls` surface with the accepted teal/neutral token treatment without changing behavior or copy.
5. Correct the Planner legend/swatch so its visual state matches the represented series and remains visible.
6. Strengthen the palette guard to normalize common hex/rgb/rgba forms, scan all relevant active presentation source in this candidate and reject unapproved green-family surfaces/tokens. Exclude only deliberate source constants inside the guard itself.

Do not redesign layout, motion, copy, routing, data/state or behavior. Operations Reports is absent from this candidate and remains a later aggregate adoption; do not merge it here.

## Writable paths only

- `app/globals.css`
- `components/tnp/public/HomeHero.tsx`
- `components/tnp/public/PavilionScene.tsx`
- `components/tnp/public/Home.module.css`
- `components/tnp/public/Public.module.css`
- `components/tnp/portals/client/ClientExperience.module.css`
- `components/tnp/portals/client/ClientStatusHub.module.css`
- `components/tnp/portals/planner/PlannerPortal.module.css`
- `components/tnp/portals/planner/PlannerRequirements.module.css`
- `components/tnp/portals/operations/AdminOperations.module.css`
- `components/tnp/shared/PreviewControls.tsx`
- `tests/brand-teal-palette.test.mjs`

TSX edits are visual/color-only. All other application, task, status, package, provider, backend and production paths are frozen.

## Required proof

- Focused palette/public/shared/portal tests plus the complete affected suites.
- Automated contrast measurements and visible keyboard focus for every corrected state.
- Browser matrix for `/`, `/contact`, representative service/department, `/login`, `/client`, `/planner`, `/freelancer` when present and `/admin` at 1440x900, 1100x900 and 390x844.
- Inspect desktop hero, mobile/no-canvas fallback and reduced-motion source/runtime behavior; record any condition the tooling cannot force.
- No horizontal overflow, console/hydration errors or action-boundary regression.
- `npm run lint`, `npx --no-install tsc --noEmit --incremental false`, `npm run build:vercel` with only the documented single unchanged Windows `EBUSY` retry, and `git diff --check fc7746e06601ca670b09dc8dd47895c543dfec58..HEAD`.

Commit only the allowlist, ordinary non-force push only the named feature branch, stop task-owned processes, prove port3111 free and clean local/live-remote equality, then return one immutable successor SHA and exact evidence. Fresh Claude focused re-review and Kartik exact-SHA acceptance remain mandatory before architect-controlled local integration.

Do not merge/push main, deploy, configure providers or alter production.
