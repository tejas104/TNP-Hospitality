> **COMPLETED — DO NOT REUSE.** Same Claude session returned `CHANGES REQUESTED` on exact `28ecc920ffe055db0495159e90ee1a1ce3ed2226`. Resume the original Astra writer with `FREELANCER-ASTRA-ROUND3-CORRECTION.md`; a new exact-SHA Claude packet will be issued only after that writer returns an immutable successor.

SESSION TYPE: OLD SESSION `96dfef2e-af39-4af1-a619-26ed81ca128a`

DO NOT OPEN A NEW SESSION. Continue only the same independent Claude review session that returned the earlier Freelancer dispositions.

# TNP-FREELANCER-ASTRA-M1 — round-two focused fixed-SHA re-review

READ-ONLY REVIEW ONLY. DO NOT IMPLEMENT, EDIT, FORMAT, COMMIT, PUSH, MERGE, DEPLOY, CONNECT PROVIDERS OR TOUCH PRODUCTION.

The previous packet stopped at correction `c8c6d759a8d5f375e20db43ef2570defd9ad8cfc`, where you found one remaining P2 lost-response replay defect. Review the immutable round-two correction only:

- Candidate: `28ecc920ffe055db0495159e90ee1a1ce3ed2226`
- Direct parent: `c8c6d759a8d5f375e20db43ef2570defd9ad8cfc`
- Original final: `eab038055b620165ea3e1e2ef5f100db4946ff91`
- Branch: `codex/tnp-freelancer-astra-m1`
- Writer worktree: `D:\TNP-worktrees\TNP-FREELANCER-ASTRA-M1` — inspect read-only
- Expected round-two delta: exactly:
  - `components/tnp/portals/freelancer/useFreelancer.ts`
  - `components/tnp/portals/freelancer/freelancer.test.mjs`
  - `components/tnp/portals/freelancer/browser-check.mjs`

Use a clean detached D:-drive review worktree at the exact candidate. Ignore the base clone's untracked `tmp/`. First prove live remote equality, clean detached state, direct-parent ancestry, exact three-file scope, writer-worktree cleanliness and free port3108. Stop on any mismatch. Report actual session/model/effort/host/account; unavailable effort must remain unavailable rather than inferred.

Read current `AGENTS.md`, `TNP-START-HERE.md`, `docs/tasks/TNP-FREELANCER-ASTRA-M1.md`, the prior review evidence and this packet. Do not inherit the writer's verdict.

## Finding that must close

For a genuinely new applicant, simulate:

1. `registerApplicant` succeeds and creates the worker/application;
2. the client loses the response and retains the exact logical request/retry identity;
3. on retry, the worker now exists because of the first successful effect;
4. the exact retry reaches the service ledger and replays the original success receipt instead of being misclassified as a seeded pre-existing-worker attempt;
5. exactly one worker and one application exist, with no duplicate effect and truthful recovered-success feedback.

Review all three layers: rendered copy/state, hook retry/restore decision, and frozen service ledger/records. A green UI alone is insufficient.

## Mandatory regression matrix

- A worker that existed before any application for that profile remains blocked from starting a fresh application.
- Same key/request identity with a materially different payload conflicts and does not mutate records.
- Another profile/worker cannot inherit the first profile's receipt, draft or success.
- Storage denial/fallback remains truthful and does not expose raw browser errors.
- Previously cleared AA contrast, keyboard, selected-worker, application/assessment/opportunity/claim/Coming/Not Coming and reset behavior remains intact.
- No attendance, KYC, payout, production-auth or provider-delivery claim is introduced.

Run from the exact detached candidate and report actual results:

- focused Freelancer tests and applicable shared preview/request-identity tests;
- the task's browser-check matrix at 1440, 1100, 390 and 320 widths, including the lost-response retry sequence;
- `npm run lint`;
- `npx --no-install tsc --noEmit --incremental false`;
- `npm run build:vercel`, with only one unchanged retry for documented Windows `EBUSY` after stopping task-owned processes;
- `git diff --check c8c6d759a8d5f375e20db43ef2570defd9ad8cfc..28ecc920ffe055db0495159e90ee1a1ce3ed2226`.

If C: is full, use D: for the detached worktree, npm cache and temporary files; do not remove unrelated user data. Do not write review artifacts into the candidate.

Return findings first with severity, exact file/line and reproduction. End with exactly one disposition:

- `APPROVE FOR ARCHITECT-CONTROLLED INTEGRATION` for `28ecc920ffe055db0495159e90ee1a1ce3ed2226`, or
- `CHANGES REQUESTED` for that exact SHA.

Also report command/browser evidence, limitations, clean candidate and free port. Kartik's exact-SHA acceptance and P-controlled local integration remain separate. Do not merge or deploy.
