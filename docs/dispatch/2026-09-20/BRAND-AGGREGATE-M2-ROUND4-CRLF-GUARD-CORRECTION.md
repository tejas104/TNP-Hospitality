SESSION TYPE: NEW SESSION — DO NOT OPEN, RESUME OR REUSE ANOTHER TNP WRITER SESSION

Current Codex architect task `01a0bcc2-a3c0-7750-8bd9-edcabd621d62` is the sole inline writer on DESKTOP-DL9FDM7. Runtime model/effort metadata are unavailable and must not be inferred.

# TNP-BRAND-AGGREGATE-M2 — round-4 CRLF guard correction

Same Claude reviewer session `cdd20a88-eed6-465c-908d-4b7496afbff1` returned `CHANGES REQUESTED` on exact clean remote-equal `71212ea57ada4447ff34a6ffebd14e439e2dc0da` for one test-portability P1. The production focus fix is independently confirmed correct.

Use only the existing branch/worktree/port from exact baseline `71212ea57ada4447ff34a6ffebd14e439e2dc0da`. The only writable path is `tests/brand-teal-palette.test.mjs`.

Normalize line endings before the selector-block lookup and add an explicit CRLF probe so the same guard is load-bearing on LF and CRLF filesystems. Preserve the panel and workspace contrast assertions plus all detector coverage. Do not edit CSS or any production source.

Run focused/full tests, lint, explicit non-incremental TypeScript, Vercel build and diff/scope checks. After committing, reproduce focused/full tests from a fresh detached Windows checkout and prove CRLF exists on disk. Remove the verification checkout, keep ports free, and return one clean ordinarily pushed immutable successor for same-Claude focused re-review, followed by Kartik exact-successor acceptance. No main push, merge, browser rerun, deployment, provider or production action.
