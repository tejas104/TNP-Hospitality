SESSION TYPE: OLD SESSION — resume only Codex task `01a0bf1e-e769-7bb1-b30e-b2cf0f5d431a`. DO NOT OPEN A NEW CODEX SESSION OR REUSE A DIFFERENT SESSION.

# TNP-HOME-CUBE-M2 — mobile reachability, contrast and timing-test correction

This packet is PREPARED BUT NOT ACTIVE. Wait for P's explicit follow-up after the UX Foundation writer lease closes. Do not edit merely because this file exists.

Fresh independent Claude Opus 5 returned `CHANGES REQUESTED` on exact `36e5b7938df7ff79aad4905b288f0348e3a67f1d`. It otherwise verified the premium cube, exact frozen hero, original four service faces, correct 90-degree behavior, fallbacks and 6-draw-call/20-triangle runtime.

## Reserved identity and baseline

- required runtime: `gpt-6-astra` / low (`Astra light`) on `DESKTOP-DL9FDM7`;
- existing task/session: `01a0bf1e-e769-7bb1-b30e-b2cf0f5d431a`;
- exact baseline/current branch tip: `36e5b7938df7ff79aad4905b288f0348e3a67f1d`;
- branch/worktree: `codex/tnp-home-cube-m2` / `D:\TNP-worktrees\TNP-HOME-CUBE-M2`;
- owned port/fallback: `3120` / `3220`.

After P activates the packet, inspect the latest `turn_context` to prove the required runtime and report exact clean HEAD, branch, remote equality and free ports before editing. Stop if any field differs.

## Exact writable scope

- `components/tnp/public/HospitalityCube.module.css`
- `components/tnp/public/hospitality-cube-motion.test.mjs`

Every other code, image, content, hero, shared-shell, portal, package and register path is frozen.

## Required correction

1. At 390x844 and 320x844, keep all four service selectors, the pause control and every disclaimer glyph visually and pointer reachable. The fixed workspace trigger must not intercept their centre or representative edge hit points. Preserve 44px targets, `touch-action: pan-y`, no horizontal overflow and safe-area behavior.
2. Bring `.sample` and `.number` to at least 4.5:1 computed contrast on the actual teal/composited background without enlarging or changing the frozen hero composition.
3. Make `HOLD_MS === 4500`, `TURN_MS === 1500` and `RESUME_MS === 6000` explicit literal contract assertions. Re-run the reviewer's three mutations and prove each makes the focused test fail.
4. Within these same files, consolidate duplicate selector/pause declarations, provide a focus-visible indicator distinguishable from both teal and the ivory focus fill, and add a boundary test that makes the 1.25 vertical-bias intent factor load-bearing. Do not widen source behavior or ownership for optional P3 items.

## Verification and handoff

Run the focused motion tests, every independently discovered relevant public/WebGL/motion/enquiry/palette/link test, all tracked tests, lint, explicit non-incremental TypeScript, Vercel build, diff check and exact two-path scope audit. Re-run the complete four-viewport browser matrix and add `elementFromPoint` evidence for each cube control at 390x844 and 320x844 plus computed contrast evidence. Preserve all previously passing service, motion, pause, keyboard, drag/swipe, resume, reduced-motion, offscreen, WebGL-failure, transparency, teal, frozen-geometry and performance evidence.

Commit and ordinarily non-force push one clean immutable successor whose direct parent is exactly `36e5b7938df7ff79aad4905b288f0348e3a67f1d`. Return exact SHA, changed files, commands/results, browser/contrast/mutation evidence, limitations, clean/remote equality and free ports. Do not merge, integrate main, deploy or mutate providers/production. Resume the same Claude cube-review session for focused fixed-SHA re-review; Kartik's exact-successor acceptance remains separate.
