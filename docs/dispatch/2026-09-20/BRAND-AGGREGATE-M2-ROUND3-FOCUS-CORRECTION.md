SESSION TYPE: NEW SESSION — DO NOT OPEN, RESUME OR REUSE ANOTHER TNP WRITER SESSION

Do not open, resume or reuse another TNP writer session. Current Codex architect task `01a0bcc2-a3c0-7750-8bd9-edcabd621d62` is the sole inline writer for this bounded correction on DESKTOP-DL9FDM7. Runtime model/effort metadata are unavailable and must not be inferred.

# TNP-BRAND-AGGREGATE-M2 — round-3 focus-visible correction

Same Claude reviewer session `cdd20a88-eed6-465c-908d-4b7496afbff1` returned `CHANGES REQUESTED` on exact clean remote-equal `497145220636ae4fe4cbda9bb984f7d9691d3e20` for the one remaining P1 focus defect.

Use only the existing branch/worktree/port from exact baseline `497145220636ae4fe4cbda9bb984f7d9691d3e20`. Writable paths are Freelancer CSS and the palette guard only.

Add a panel-local focus-visible override that contrasts at least 3:1 with exact teal while leaving the workspace's teal-on-ivory focus rule unchanged. Regression-test both surfaces. Narrow `.color.set(...)` detection to light-named variables and add a `material.color.set(...)` negative probe; do not touch materials or 3D source. Preserve all cleared round-2 behavior and scope.

Run focused/full tests, lint, explicit TypeScript, Vercel build, diff check and genuine-Tab browser checks at 1440x900, 1100x900 and 390x844. Stop the server and return one clean ordinarily pushed immutable successor for same-Claude focused re-review, followed by Kartik exact-successor acceptance. No main push, merge, deployment, provider or production action.
