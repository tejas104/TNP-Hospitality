SESSION TYPE: NEW SESSION

DO NOT OPEN, RESUME OR REUSE THE CURRENT CODEX SESSION OR ANY OLD TNP BUILDER/REVIEW SESSION. This is a clean architect continuation for `D:\TNP Hospitality`.

# TNP Hospitality — master architect handoff after Brand Aggregate M2

You are the single inline architect/developer. Do not spawn development subagents. The user wants substantial frontend progress with token efficiency, but repository gates and exact ownership still apply. Homepage redesign remains last. Do not push main, deploy, change providers/production, move money or use real personal data unless the user later gives explicit action-specific authority.

## Authority and current user direction

- Kartik is the sole human fixed-SHA acceptance owner.
- Codex-authored work receives fresh Claude review; sensitive architecture/security/data/finance also retains independent Sol review where the repository requires it.
- User permits `gpt-6-astra` up to medium for very complex frontend/UI work, but requires inline execution rather than subagent development. Never claim an actual model/effort unless the runtime exposes it.
- Preserve the accepted public site. Finish the frontend except the main homepage; do the homepage only after the portal/shared work.
- Client redesign should use Wispr Flow only as a product-clarity benchmark, not as copied trade dress: calm ivory canvas, concise promise, interactive four-product demonstration, purposeful depth/motion and TNP's exact brand system.
- Replace the inherited fixed right-edge semicircle workspace control through UX Foundation, not a Client-only workaround.
- All records remain labelled synthetic previews until real backend/auth/provider work is separately authorized and verified.

## Verified repository state at handoff

- Primary clone: `D:\TNP Hospitality`
- Branch: `main`
- Local main contains, in order:
  - accepted Brand merge `b56ad987735a8a3ad909c35ede974a2e7353f8b3`;
  - Brand integration record `14fa826cee42612b54ffb339219eb9b6a195226c`;
  - Aggregate M2 launch `99b4c98c0888ea1b3703830356386b71b7152149`;
  - Aggregate fixed-handoff record `677a3dd48d0faa27e521b0d78ce1f43cc789b775`;
  - the commit carrying this handoff file may be the current local tip.
- `origin/main` was `fe277f29a41e4a9f078da9b86e42e1037e4583bc` at handoff and was an ancestor of local main. Main was not pushed.
- Preserve untracked `output/` and `tmp/`; they belong to the workspace/user. Do not clean, reset, delete or stage them.
- Accepted Brand candidate `6f3e48d0a7d9bd9b413b1540f4ad8a645440edb3` is already locally integrated. That authorization did not permit a main push or deployment.

### Current review candidate

- Task: `TNP-BRAND-AGGREGATE-M2`
- Branch: `codex/tnp-brand-aggregate-m2`
- Worktree: `D:\TNP-worktrees\TNP-BRAND-AGGREGATE-M2`
- Launch/direct parent: `99b4c98c0888ea1b3703830356386b71b7152149`
- Candidate: `d5c15be0be3acf984206a0a29c3f16dabe522bdf`
- Live remote equality was verified after ordinary non-force push.
- Exact candidate delta:
  - `components/tnp/portals/freelancer/FreelancerPortal.module.css`
  - `components/tnp/portals/operations/AdminOperations.module.css`
  - `tests/brand-teal-palette.test.mjs`
- Writer checkout was clean and port3117 was free.
- Fresh Claude packet: `docs/dispatch/2026-09-20/BRAND-AGGREGATE-M2-CLAUDE-FIXED-SHA-REVIEW.md`
- Claude has not yet returned a disposition and Kartik has not yet accepted exact `d5c15be...`. Therefore do not integrate it yet.

### Candidate implementation and evidence

- Freelancer `--deep` is exact `#008080`; neutral `--ink` owns text/form/border uses; true panels remain teal; `.updated` uses a neutral overlay.
- Operations Reports export status uses `rgb(0 128 128 / 9%)`.
- Guard now detects JS style-object named greens, JSX fill/stroke named greens, numeric hexadecimal colours in supported Three.js light constructors, and measures contrast against actual beige `#e5e1cd`.
- Mutation probes cover the new parser cases.
- Focused guard 6/6 PASS; 18 discovered files / 118 tests PASS; lint PASS with only the three inherited React-compiler warnings at `hooks/use-mobile.ts:16` and `components/tnp/AppShell.tsx:79,116`; explicit non-incremental TypeScript PASS; first-attempt Vercel build PASS; diff check PASS.
- Real-browser checks covered `/freelancer` and `/admin` Reports at 1440x900, 1100x900 and 390x844. No horizontal overflow or warn/error console output. Keyboard focus was visible. CSV feedback computed to `rgba(0, 128, 128, 0.09)` with neutral ink and wrapped within the mobile client width.
- The fixed PreviewControls and side workspace control are inherited shared-shell issues reserved for UX Foundation, not Aggregate M2 defects.

## Required startup preflight

Read completely before acting:

1. `TNP-START-HERE.md`
2. `AGENTS.md`
3. `docs/PRODUCT.md`
4. `docs/DESIGN.md`
5. `docs/STATUS.md`
6. `docs/LANES.md`
7. `docs/REVIEW-CADENCE.md`
8. `docs/LAUNCH-PROTOCOL.md`
9. `docs/tasks/TNP-BRAND-AGGREGATE-M2.md`
10. `docs/CLIENT-WISPR-INSPIRED-REDESIGN-BRIEF.md`
11. `docs/PORTAL-ROLE-SCOPE-REFINEMENT.md`
12. `docs/tasks/TNP-UX-FOUNDATION-M1.md`
13. `docs/contracts/UX-SURFACE-ELEVATION.md`
14. `docs/tasks/TNP-CLIENT-PLANNER-CORRECTED-M2.md`

Then perform read-only verification:

- prove PWD, branch and HEAD in the primary clone;
- inspect `git status --short` and preserve `output/` / `tmp/`;
- fetch remote refs without pulling or rewriting local history;
- verify `origin/main` remains an ancestor of local main or stop and report divergence;
- verify live feature remote equals full candidate `d5c15be...`;
- verify `d5c15be...^` is exact Launch `99b4c98c...` and the delta is exactly the three allowlisted paths;
- verify the candidate worktree is clean and port3117 is free;
- verify whether a fresh Claude disposition and Kartik exact-SHA acceptance have actually been supplied. Conversation claims without the exact SHA or a pasted reviewer disposition are not enough.

## Immediate execution sequence

### Gate A — Claude and Kartik disposition

1. Give the user the exact fresh-Claude packet if they have not run it.
2. If Claude returns `CHANGES REQUESTED`, record findings first and create a bounded correction contract/lease. Do not edit directly on main and do not broaden into UX Foundation.
3. If Claude approves exact `d5c15be...` but Kartik has not accepted that exact SHA, stop integration and ask only for exact-SHA acceptance.
4. Only when both gates are present may the architect locally integrate.

### Gate B — architect-controlled local integration

Before merging, reverify main status, remote equality, ancestry, exact scope, worktree cleanliness and free ports. From local main use a no-fast-forward, no-commit merge of exact `d5c15be...`; inspect the staged result before committing. The merge should introduce only the candidate's three source/test files because the launch/docs history is already on local main. Resolve nothing by overwriting unrelated work.

After the local merge, with no main push:

- run the focused palette guard;
- independently discover/run every project test;
- run lint, explicit `npx --no-install tsc --noEmit --incremental false`, Vercel build and diff check;
- browser-smoke all currently integrated public/access/workspace routes at desktop and mobile, with special attention to Freelancer, Operations Reports, inherited PreviewControls, the side workspace control, overflow and console/hydration output;
- record merge SHA, parents, commands/results, limitations and closed lease in serialized docs;
- keep main local-only.

### Gate C — prepare and execute UX Foundation M1

Only after Aggregate M2 is approved, accepted, integrated and aggregate checks pass may `TNP-UX-FOUNDATION-M1` move from Draft to Ready. Create a launch-contract commit separate from its source baseline. Record actual model/effort/host/worktree/port/reviewers and exact ownership. Since the user requires inline development, do not create a development subagent; the active session itself must satisfy the verified model gate or stop before source edits.

UX Foundation is the next large frontend milestone. Its critical outcomes are:

- explicit marketing/access/workspace/guest route classification;
- one typed route/workspace manifest;
- truthful named synthetic demo profiles with tab isolation and memory fallback;
- `/operations` canonical route plus `/admin` compatibility;
- compact header current-workspace pill with keyboard popover on desktop and safe-area mobile sheet/menu;
- complete removal of the fixed right-edge semicircle dial from workspace routes;
- non-obscuring PreviewControls and warnings at 390px, 200% zoom and safe-area layouts;
- modern raised workspace-access cards with visible resting affordance, focus/active/touch/reduced-motion parity;
- reusable orientation, dominant-next-action, attention, semantic action, tabular-numeric, density and elevation primitives with at least three real shared consumers;
- evidence-safe shared `PartnerCard` for Venues and approved TNP Planners;
- homepage source remains frozen except regression proof for cursor mounting/unmounting.

Do not widen UX Foundation into portal-local Client/Planner/Freelancer/RSVP features, backend auth, database, payments or providers. Return one immutable feature candidate to fresh Claude and Kartik review; do not self-certify or locally integrate it without both gates.

### Gate D — Client/Planner corrected M2 after UX Foundation

After UX Foundation is reviewed, accepted and locally integrated, launch `TNP-CLIENT-PLANNER-CORRECTED-M2` from the clean aggregate source. Follow `docs/CLIENT-WISPR-INSPIRED-REDESIGN-BRIEF.md`:

- ivory, product-led Client first view rather than the rejected full-teal palace poster;
- concise promise and one dominant `Build my event brief` action;
- immediately understandable interactive chooser for Hospitality, Venue, TNP Planner and RSVP in any combination;
- never force Venue or Planner when a visitor wants direct Hospitality or RSVP;
- keep palace/event imagery lower in the story and use only attributable or explicitly illustrative assets;
- preserve TNP exact teal `#008080`, ivory, champagne, neutral ink and editorial typography;
- client can submit a low-friction request without login, while approved Client access remains Admin-granted;
- distinguish a client-appointed representative from an experienced Admin-approved and event-assigned TNP Planner;
- Planner registration must be low friction: name, company, phone, email, optional GST and selectable Hospitality/RSVP/Venue requirements;
- preserve quotation, grant, profile, notes and status truth; do not imply backend authorization from a UI preview.

The broader platform scope remains recorded, not silently implemented in a frontend slice: Main Admin/scoped co-admin capabilities; editable product/service/venue/planner/partner catalog; multi-role freelancer requirements; planner/admin notes; event/date overlap and selection notices; attendance/location evidence and planner monitoring; bank details and Razorpay-backed payout architecture; reminder/replacement rules; RSVP timing grants; printable invoices/receipts; exports; responsive Android/iOS browsers and mobile Lighthouse target above 80. These require their own contracts, backend/security/data/finance/provider gates. Do not fake them as production-complete UI.

## Hard prohibitions

- No push of `main`.
- No deployment, Vercel/provider/production mutation or payment action.
- No framework/database/provider migration.
- No reuse, reset or cleanup of the historical `TNP-CLIENT-ASTRA-M1` worktree with preserved Planner WIP.
- No deletion or staging of `output/` or `tmp/`.
- No real identity, bank, location, KYC, guest or payment data in source, fixtures, screenshots or logs.
- No claim that synthetic UI satisfies server authorization, allocation atomicity/overlap, attendance evidence, audit, idempotent finance, notification delivery or provider approval.
- No homepage redesign until the shared and portal frontend sequence is complete and separately authorized.

## Reporting format for the new session

Lead with the verified current outcome. Separate repository facts from requested values and preview behavior from production capability. For each completed milestone report exact changed files, immutable SHA, parent, remote equality, commands/results, browser matrix, inherited warnings/limitations, stopped servers/free ports, reviewer disposition and whether main remains local-only. When blocked by a gate, continue any safe non-overlapping read-only preparation but do not cross the gate.
