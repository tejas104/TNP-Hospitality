SESSION TYPE: OLD SESSION `local_caafefbd-0c4e-46b4-a5e8-a9a989d8b7bf`

DO NOT OPEN A NEW SESSION. Resume only the same Claude Code desktop review session that returned `CHANGES REQUESTED` on exact `1c5204f0894775292b633824118c58a5df9454b7`.

# TNP-BRAND-TEAL-M1 — round-four focused fixed-SHA re-review

READ-ONLY REVIEW ONLY — DO NOT IMPLEMENT. Review exact candidate:

- predecessor reviewed candidate: `1c5204f0894775292b633824118c58a5df9454b7`;
- round-four candidate: `6f3e48d0a7d9bd9b413b1540f4ad8a645440edb3`;
- branch: `codex/tnp-brand-teal-m1`;
- writer worktree: `D:\TNP-worktrees\TNP-BRAND-TEAL-M1`;
- review port: use a new free review-only port, never 3111.

First report actual session/model/effort/host/account/PWD. Verify live remote equals the full round-four candidate, its direct parent equals `1c5204f...`, the delta is exactly the five files below, the writer checkout is clean, and writer/review ports are free. Use a clean detached review worktree. Do not edit, commit, push, merge, deploy or change providers/production.

## Exact changed-file allowlist

- `app/globals.css`
- `components/tnp/portals/client/ClientExperience.module.css`
- `components/tnp/portals/operations/AdminOperations.module.css`
- `components/tnp/public/PavilionScene.tsx`
- `tests/brand-teal-palette.test.mjs`

The only authorized production changes are contrast/focus/interaction-state corrections and visual material correction. The test may expand palette parsing and focused regression coverage. Confirm no route, product copy, domain/data/state, contract, package, provider or production behavior change.

## Required finding closure

Reproduce each prior blocker rather than inheriting writer evidence:

1. All fourteen formerly white-on-beige Operations section kickers must compute to accessible teal on beige. Teal-surface headings remain white.
2. `RUN SAMPLE WORKER CLAIM` must receive a visible teal focus ring on beige; verify keyboard focus, not only static CSS.
3. The live WebGL homepage hero must no longer contain the approximately `#004137` dark-green material cast. Inspect the rendered scene and perform the same or stronger pixel/color evidence used in the prior review. Also verify no-canvas and reduced-motion fallbacks.
4. Palette mutation probes must reject HSL, OKLab/OKLCH, named CSS greens and Three.js numeric/array encodings without false matching identifier prose.
5. Verify the Client skip-link focus indicator, beige secondary text, pressed states and visually distinct disabled Operations navigation.

## Required regression verification

- Independently discover and run every test file present in the exact candidate checkout, then run focused palette mutation probes, lint, explicit `npx --no-install tsc --noEmit --incremental false`, Vercel build and `git diff --check 1c5204f0894775292b633824118c58a5df9454b7..6f3e48d0a7d9bd9b413b1540f4ad8a645440edb3`.
- Browser-check `/`, `/client` and `/admin` at 1440x900, 1100x900 and 390x844. Recheck other routes if the cumulative brand correction or guard reveals a regression.
- Exercise keyboard focus, hover/pressed/disabled states, reduced motion and forced WebGL-unavailable fallback where tooling permits.
- Measure actual foreground/background or focus/background contrast for every sampled corrected state. Confirm no horizontal overflow and no console/hydration errors.
- Confirm process cleanup, free ports and clean review/writer worktrees.

## Evidence to reproduce, not inherit

The inline architect reports exact five-file scope, clean pushed remote equality, direct-parent provenance, 21/21 tests discovered in this candidate checkout, lint with three unchanged inherited React-compiler warnings, explicit non-incremental TypeScript, first-attempt Vercel build and diff check PASS. Rendered inspection measured corrected Operations beige-surface labels/focus at approximately 4.821:1, white-on-teal labels at approximately 4.773:1 and the Client black-on-teal focus indicator at approximately 4.399:1. A visible homepage inspection showed the teal scene without the prior obvious dark-green cluster. The available writer browser could not control the complete required viewport matrix or directly sample WebGL canvas pixels; these remain independent-review obligations.

Return findings ordered P0-P3 with exact file/line references, commands/results, rendered evidence, limitations and one disposition: `APPROVE FOR ARCHITECT-CONTROLLED INTEGRATION` or `CHANGES REQUESTED`. Then stop. Kartik exact acceptance of `6f3e48d0a7d9bd9b413b1540f4ad8a645440edb3` remains a separate gate.
