SESSION TYPE: CURRENT NEW ARCHITECT SESSION

Do not open, resume or reuse another TNP writer session. Current Codex architect task `01a0bcc2-a3c0-7750-8bd9-edcabd621d62` is the sole inline writer for this correction on DESKTOP-DL9FDM7. Runtime model/effort metadata are unavailable and must not be inferred.

# TNP-BRAND-AGGREGATE-M2 — round-2 accessibility and guard correction

Fresh Claude session `cdd20a88-eed6-465c-908d-4b7496afbff1` returned `CHANGES REQUESTED` on exact clean remote-equal `d5c15be0be3acf984206a0a29c3f16dabe522bdf`.

Use only:

- branch `codex/tnp-brand-aggregate-m2`;
- worktree `D:\TNP-worktrees\TNP-BRAND-AGGREGATE-M2`;
- baseline `d5c15be0be3acf984206a0a29c3f16dabe522bdf`;
- port 3117 only after confirming it is free.

Writable paths:

- `components/tnp/portals/freelancer/FreelancerPortal.module.css`;
- `tests/brand-teal-palette.test.mjs`.

Correct the measured text and non-text contrast inside `.journey` and `.opportunityDetail` on exact `#008080`, preserving hierarchy, behavior, markup and all responsive states. Mutation-lock the actual beige substrate, reject `data-*` false positives, and detect the demonstrated numeric `.color.set(...)` case. Do not touch Operations CSS, TSX, behavior, routes, copy, shared shell, homepage, Client/Planner/RSVP, packages, providers or production code. Do not widen into Fog, materials, 3D redesign, disabled/pressed redesign, PreviewControls, the workspace switcher or UX Foundation.

Run focused and full tests, lint, explicit non-incremental TypeScript, Vercel build and diff check. Browser-check Freelancer at 1440x900, 1100x900 and 390x844, including computed contrast, keyboard focus, overflow and console output. Stop the server and return one clean ordinarily pushed immutable successor for focused re-review by the same Claude session and then Kartik exact-successor acceptance. No main push, merge, deployment, provider or production action.
