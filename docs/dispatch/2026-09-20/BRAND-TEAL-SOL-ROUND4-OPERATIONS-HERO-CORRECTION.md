SESSION TYPE: NEW SESSION `/root/brand_teal_sol_round4`

Use only the assigned `gpt-5.6-sol` / xhigh runtime for this complex correction. Do not substitute Astra or another model.

# TNP-BRAND-TEAL-M1 — round-four Operations contrast and live-hero correction

IMPLEMENTATION CORRECTION ONLY. Start from exact clean live-remote-equal candidate `1c5204f0894775292b633824118c58a5df9454b7` on `codex/tnp-brand-teal-m1` in `D:\TNP-worktrees\TNP-BRAND-TEAL-M1`. Re-prove PWD, branch, HEAD, live remote, cleanliness, source ancestry and port3111 free before editing. Do not pull, reset, clean, rebase, recreate the worktree or touch untracked user files.

Kartik has accepted exact `1c5204f...`, but the independent Claude reviewer returned `CHANGES REQUESTED`. This correction closes those findings; it is not integration authority. The successor requires the same Claude session's focused approval and Kartik's exact-successor acceptance before P integration.

## Required corrections

1. **Operations card kickers:** fourteen kickers currently inherit white on beige at approximately 1.31:1. Narrow the global white-on-teal rule or add the smallest explicit beige-card/live-panel override so `.decisionCard`, `.controlForm`, `.evidenceCard`, `.auditPanel` and `.live-ops-panel` kickers meet at least 4.5:1. Preserve white kickers on actual teal surfaces.
2. **Operations focus:** `RUN SAMPLE WORKER CLAIM` currently receives a white focus ring on beige at approximately 1.31:1. Give controls on `.live-ops-panel` a focus-visible indicator of at least 3:1 without weakening teal-surface focus.
3. **Live WebGL hero cast:** source teal material literals still render an approximately `#004137` dark-green cluster covering 11.3% of opaque scene pixels. Adjust only visual material/roughness/metalness/environment-independent lighting values so the live desktop scene visibly reads as the approved teal family, while retaining composition, motion, one-canvas policy and mobile/reduced-motion/no-WebGL/context-loss fallbacks. Do not hide the issue behind a fallback or change scene behavior.
4. **Guard completeness:** extend deterministic palette checks/mutation probes to reject the reviewed alternate-color encodings relevant to active presentation source: `hsl()`/`hsla()`, `oklch()`/`oklab()`, disallowed named green colors and Three.js numeric/array color literals. Avoid broad false positives in ordinary data, IDs or documentation; scan only agreed active presentation paths and parse/normalize where necessary.
5. **Bounded P3 accessibility while owned files are open:** strengthen the Client skip-link focus indicator to at least 3:1; bring beige-card normal secondary text to at least 4.5:1; add a distinct non-motion pressed state for affected current controls; and make unavailable Operations navigation visually distinct in addition to text/native disabled semantics. Preserve reduced-motion, keyboard and touch behavior.

## Writable paths only

- `app/globals.css`
- `components/tnp/portals/client/ClientExperience.module.css`
- `components/tnp/portals/operations/AdminOperations.module.css`
- `components/tnp/public/PavilionScene.tsx`
- `tests/brand-teal-palette.test.mjs`

All other files are frozen, including Client status CSS, Planner/Freelancer/RSVP/Reports files, AppShell logic, routes, copy, data/state, services/contracts/fixtures, packages/lockfiles, providers, server code and docs/registers. TSX changes in `PavilionScene.tsx` must remain visual/material/light constants only. Stop and report if a behavior or extra-file change appears necessary.

## Verification

- Run the focused palette guard and all fifteen discovered tests.
- Add deterministic regression coverage for Operations beige-card kicker/focus contrast, Client skip-link focus, secondary text, pressed/disabled differentiation and new palette encodings.
- Run `npm run lint`, `npx --no-install tsc --noEmit --incremental false`, `npm run build:vercel`, and `git diff --check 1c5204f0894775292b633824118c58a5df9454b7..HEAD`. Only one unchanged Windows Nitro `EBUSY` retry is permitted after stopping task-owned processes.
- Prove the exact five-file allowlist.
- Render `/admin` at 1440x900, 1100x900 and 390x844. Measure every affected kicker and the sample-claim focus ring; exercise focus, pressed and unavailable navigation states.
- Render `/client` at 1440x900 and 390x844 and measure the skip-link focus indicator while confirming the status-hub kicker remains white on teal at at least 4.5:1.
- Render the live WebGL homepage at 1440x900 and measure pixels with the reviewer's preserve-buffer method or an equivalently reproducible method. Also verify mobile still, reduced-motion where tooling permits, forced no-WebGL and context-loss fallbacks.
- Recheck all nine previously reviewed routes for palette/contrast regression, horizontal overflow, console and hydration errors.

## Handoff

Commit only the five allowed files, ordinary non-force push only `codex/tnp-brand-teal-m1`, prove clean live remote equality, stop browser/server and prove task/review ports free. Return one immutable successor with exact changed files, commands/results, mutation probes, computed contrast, rendered-pixel evidence and limitations.

The same Claude review session `local_caafefbd-0c4e-46b4-a5e8-a9a989d8b7bf` performs a focused cumulative fixed-SHA re-review. Kartik exact-successor acceptance and P-controlled local integration follow. Do not merge/push main, deploy or change providers/production.
